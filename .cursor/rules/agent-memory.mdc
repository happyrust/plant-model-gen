---
description: "Agent memory protocol — append diary after each task"
alwaysApply: true
---

# Agent 记忆协议

强制要求，违反视为任务未完成。

每次完成任务后，必须向项目日记追加一条记录。

路径：`{project_root}/.memory/{YYYY-MM-DD}.md`（按当天日期命名，文件不存在则新建）

**`{project_root}` 自动识别规则**：
1. 找到本次任务实际修改（或读取/分析）的文件路径
2. 取该文件所在的 **git 仓库根目录**（即含 `.git/` 的最近祖先目录）作为 `{project_root}`
3. 将记录写入 `{project_root}/.memory/`，目录不存在则创建
4. 同时修改多个 git 仓库的文件 → 每个仓库各写一条
5. 若文件不属于任何 git 仓库，则回退写入当前工作区主目录的 `.memory/`

条目格式：

```
## {HH:MM} [{agent_name}] {标题}
- 完成：（一句话描述本次任务结果）
- 决策：（关键选择及原因，无则省略）
- 待办：（下一步行动，无则省略）
```

`{标题}` 为本次任务的简短总结（5-15字），例如：`更新 memory 规则按项目归档`、`实现空间查询 API`。

规则：
1. 每次任务完成后立即追加，不可延迟或遗漏
2. 文件按当天日期命名，不存在则新建
3. `{agent_name}` 使用当前 MCP 编号（如 MCP-2、MCP-3）或窗口标识
4. `.memory/` 目录纳入 git 跟踪
