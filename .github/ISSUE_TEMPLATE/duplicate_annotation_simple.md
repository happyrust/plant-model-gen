---
name: 防重复确认保存问题
about: 校审流程中确认按钮可无限点击导致数据重复
---

## 问题描述

在校审/提资流程中，确认按钮可以无限次点击，每次都会向数据库写入一条新的确认记录，导致：

1. **数据无限膨胀** - 同一任务同一操作员可以产生任意数量的重复记录
2. **历史混乱** - 无法区分哪些是"真正的新确认"，哪些是"重复点击"
3. **体验差** - 用户不知道是否已经保存成功，反复点击确认

## 复现步骤

1. 打开校审面板，添加批注或测量
2. 点击"确认当前数据"按钮
3. 不做任何修改，再次点击确认按钮
4. 重复步骤3多次
5. 查询数据库，发现同一任务同一操作员产生了多条完全相同的记录

## 期望行为 vs 实际行为

| 场景 | 期望 | 实际 |
|------|------|------|
| 首次确认 | 创建新记录 | ✅ 创建新记录 |
| 重复确认（内容未变） | 返回已有记录，不新增 | ❌ 创建新记录 |
| 修改后再次确认 | 更新原有记录，刷新时间 | ❌ 创建另一条新记录 |
| 删除所有批注后确认 | 允许确认空状态 | ❌ 面板直接消失无法确认 |

## 解决方案概述

1. **稳定槽位键** - 用 `form_id + current_node + operator_id` 确定唯一记录位置
2. **内容快照哈希** - 对确认内容计算 SHA256，检测是否真的变化
3. **幂等 Upsert** - 后端实现 `UPSERT` 语义：存在则更新，不存在则创建，内容相同则 no-op
4. **前端状态分离** - 区分"已确认状态"与"当前草稿"，用快照对比检测未保存变更

## 验证结果

真实链路测试通过，详见下方截图。

## 相关改动

- **后端**: `src/web_api/review_api.rs` - 重构 `create_record` 为幂等 upsert
- **前端**: `src/components/review/ReviewPanel.vue` - 快照对比控制按钮状态
- **前端**: `src/components/review/ReviewConfirmation.vue` - 同步确认逻辑
- **新增**: `debug_scripts/review_record_dedup/validate_review_record_dedup.py` - 验证脚本
