<!-- Generated by SigMap gen-context.js v8.21.0 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## todos
```
src\fast_model\export_model\export_prepack_lod.rs:4244  # TODO: 从 pe 表查询 owner 的 owner_type
src\fast_model\export_model\export_prepack_lod.rs:5480  # TODO: 从 cache 中获取 owner 的 owner_type
src\fast_model\gen_model\query_compat.rs:186  # TODO: 需要实现 CATE 查询逻辑
src\fast_model\gen_model\utilities.rs:25  # TODO: 需要从原来的 E3D_DEBUG_ENABLED 获取
src\fast_model\gen_model\utilities.rs:38  # TODO: 需要从原来的 E3D_INFO_ENABLED 获取
src\fast_model\gen_model\utilities.rs:51  # TODO: 需要从原来的 E3D_TRACE_ENABLED 获取
src\lib.rs:396  # TODO: 还有个问题，可能需要通过队列来排队任务
src\lib.rs:519  # TODO: Fix this function call
src\versioned_db\database.rs:756  # TODO: 改成一对多的实现
src\versioned_db\database.rs:840  # TODO: 需要实现create_tables函数或使用现有的表创建逻辑
src\versioned_db\database.rs:2316  # TODO: 按照文件大小排序，只有小于多少的能开启多线程，模型一大就不合适了
src\web_server\handlers.rs:2150  # TODO: 从数据库获取 noun
src\web_server\handlers.rs:6334  # TODO: Implement proper PDMS sesno extraction
```

## src

### src\cli_modes.rs
```
pub struct ExportConfig  :37-66  # 统一的导出配置结构体
pub struct RoomComputeCliConfig  :4245-4254  # 房间计算配置
pub struct SpatialQueryVerifyResultItem  :4894-4897
pub struct SpatialQueryVerifySnapshot  :4900-4910
impl ExportConfig  :68-87
impl ExportConfig  :89-273
  pub fn new(refnos_str: Vec<String>) → Self  :91-91  # 创建新的导出配置
  pub fn with_output_path(mut self, output_path: Option<String>) → Self  :100-100  # 设置输出路径
  pub fn with_filter_nouns(mut self, filter_nouns: Option<Vec<String>>) → Self  :106-106  # 设置过滤类型
  pub fn with_include_descendants(mut self, include_descendants: bool) → Self  :112-112  # 设置是否包含子孙节点
  pub fn with_unit_conversion(mut self, source_unit: &str, target_unit: &str) → Self  :118-118  # 设置单位转换
  pub fn with_verbose(mut self, verbose: bool) → Self  :125-125  # 设置详细输出
  pub fn with_regenerate_plant_mesh(mut self, regenerate_plant_mesh: bool) → Self  :131-131  # 设置重新生成 plant mesh
  pub fn with_run_all_dbnos(mut self, run_all_dbnos: bool) → Self  :137-137  # 设置是否默认跑全库
impl RoomVerifySummary  :1165-1178
impl ScopedEnvVar  :1781-1788
impl ScopedEnvVar  :1790-1796
impl GenerateProgressHeartbeat  :1804-1820
impl GenerateProgressHeartbeat  :1822-1826
impl RoomComputeCliReport  :5315-5387
pub fn kill_process_on_port(port: u16)  :379-392  # 关闭占用指定端口的进程（避免 file 模式下 RocksDB 排他锁冲突）。
pub async fn ensure_surreal_connected(db_option_ext: &DbOptionExt) → Result<()>  :491-555  # 连接 SurrealDB（固定输入数据源）。
pub async fn diagnose_surreal_startup_mode(db_option_ext: &DbOptionExt) → Result<()>  :631-723
pub async fn room_verify_json_mode(input: &Path, db_option_ext: &DbOptionExt) → Result<()>  :1390-1429
pub async fn room_verify_json_mode(_input: &Path, _db_option_ext: &DbOptionExt) → Result<()>  :1432-1436
```

### src\data_interface\sesno_increment.rs
```
pub struct PdmsSesnoIncrementFileReport  :156-173
pub struct PdmsSesnoElementChange  :176-192
pub struct PdmsSesnoIncrementOutcome  :195-199
pub struct PdmsSesnoCollectedFile  :202-206
pub struct PdmsSesnoCollectedOutcome  :209-212
pub struct VersionAnchorRecord  :216-227  # specs/022：已写入 `sesno_version_anchor` 表的锚点记录（用于汇总 JSON 与日志）。
pub struct VersionCommitFailureRecord  :230-235
pub struct PdmsIncrementPersistStats  :238-260
impl PdmsIncrementPersistStats  :262-277
impl PdmsSesnoIncrementOutcome  :279-307
  pub fn merge(&mut self, other: PdmsSesnoIncrementOutcome)  :280-280
  pub fn total_session_count(&self) → usize  :300-300
  pub fn total_element_count(&self) → usize  :304-304
impl PdmsSesnoCollectedOutcome  :309-318
  pub fn merge(&mut self, other: PdmsSesnoCollectedOutcome)  :310-310
  pub fn into_outcome(self) → PdmsSesnoIncrementOutcome  :315-315
impl OperationModelImpact  :540-544
pub fn refresh_db_meta_for_increment_files(project: &str, files: &[PdmsSesnoIncrementFileReport],) → anyhow::Result<usize>  :463-516  # 将本次 pdms-io 增量文件的 ref0/dbnum 信息刷新到 db_meta_info
pub async fn persist_pdms_increment_files(files: &[PdmsSesnoIncrementFileReport], detail: bool,) → anyhow::Result<PdmsIncremen...  :1204-1213
pub async fn persist_collected_pdms_increment_files(files: &[PdmsSesnoCollectedFile], recover_pending: bool,) → anyhow::Result<PdmsIncremen...  :1215-1245
pub fn collect_pdms_increment_for_file_with_operations_options(project: &str, file_path: impl AsRef<Path>, cached_sesno: u32, to_sesno: Option<u32>, detail: bool, model_impact_filter: bool,) → anyhow::Result<PdmsSesnoCol...  :1252-1411  # 使用 pdms-io 直接从一个 E3D/PDMS db 文件按 sesno 范围收集增量。
pub fn collect_pdms_increment_for_file_with_operations(project: &str, file_path: impl AsRef<Path>, cached_sesno: u32, to_sesno: Option<u32>, detail: bool,) → anyhow::Result<PdmsSesnoCol...  :1489-1504
pub fn collect_pdms_increment_for_file(project: &str, file_path: impl AsRef<Path>, cached_sesno: u32, to_sesno: Option<u32>, detail: bool,) → anyhow::Result<PdmsSesnoInc...  :1506-1521
pub fn collect_pdms_increment_for_dbnums_from_index_with_operations_options(project: &str, index_path: impl AsRef<Path>, dbnums: &[u32], cached_sesno: u32, to_sesno: Option<u32>, detail: bool, model_impact_filter: bool,) → anyhow::Result<PdmsSesnoCol...  :1525-1565  # 通过 db_index
pub fn collect_pdms_increment_for_dbnums_from_index_with_operations(project: &str, index_path: impl AsRef<Path>, dbnums: &[u32], cached_sesno: u32, to_sesno: Option<u32>, detail: bool,) → anyhow::Result<PdmsSesnoCol...  :1568-1585
```

### src\fast_model\gen_model\gen_pipeline.rs
```
pub fn validate_sjus_map(sjus_map: &DashMap<RefnoEnum, (Vec3, f32) → Result<()>  :48-64  # 验证 SJUS map 是否完整
pub async fn gen_pipeline_geos(db_option: Arc<DbOptionExt>, generation_read: Arc<GenerationReadContext>, config: &GenPipelineConfig, sender: flume::Sender<ShapeInstancesData>, seed_roots: Option<Vec<RefnoEnum>>, generation_artifacts: Option<Arc<GenerationArtifacts>>,) → Result<CategorizedRefnos>  :82-107
```

### src\fast_model\gen_model\model_writer.rs
```
pub struct GenerationArtifacts  :27-35  # 单次模型生成的内存事实源。生成、mesh、关系与 boolean 阶段只在这里交接，
pub struct GenerationArtifactsSummary  :47-57
pub struct ModelWriterStageReport  :311-316
pub struct ModelWriterContractEvidence  :348-357
pub struct DrainOnlyStats  :360-371
pub struct ModelWriteBatchReport  :410-412
pub struct ModelWriterFinishReport  :415-419
pub struct BooleanBridgeRequest  :421-429
pub struct BooleanBridgeReport  :432-438
pub struct SurrealModelWriterBackend  :540-544
pub struct DrainOnlyModelWriterBackend  :840-844
pub enum ModelWriterStageStatus  :304-308
pub trait ModelWriterBackend  :441-441
impl GenerationArtifacts  :59-205
  pub fn new(authoritative_snapshot_id: u64) → Self  :60-60
  pub fn record_base_batch(&self, batch_id: u64, shape_insts: Arc<ShapeInstancesData>,) → anyhow::Result<()>  :72-76
  pub fn record_mesh_results(&self, batch_id: u64, mesh_results: &HashMap<u64, MeshResult>,) → anyhow::Result<()>  :93-97
  pub fn record_missing_neg_carriers(&self, refnos: impl IntoIterator<Item = RefnoEnum>,) → anyhow::Result<()>  :110-113
  pub fn take_run_outputs(&self) → anyhow::Result<(Vec<Boolean...  :121-121
  pub fn missing_neg_carriers(&self) → anyhow::Result<Vec<RefnoEnum>>  :131-131
  pub fn record_tubi_info(&self, values: &DashMap<String, TubiInfoData>) → anyhow::Result<()>  :141-141
  pub fn tubi_info(&self) → &DashMap<String, TubiInfoData>  :159-159
impl ModelWriterStageReport  :318-345
  pub fn executed(stage: &'static str, item_count: usize) → Self  :319-319
  pub fn implemented(stage: &'static str) → Self  :328-328
```

### src\fast_model\gen_model\pdms_inst.rs
```
pub struct SaveInstanceDataReport  :123-125
pub struct InstRelatePrecomputed  :2920-2929  # inst_relate 中 fn::* 的预计算结果缓存
impl TransactionBatcher  :2161-2295
impl InstRelatePrecomputed  :2931-3062
  pub fn from_generation_read(read: &super::context::GenerationReadContext,) → anyhow::Result<Self>  :2932-2934
  pub async fn build(refnos: &[RefnoEnum]) → Self  :2967-2967  # 从 TreeIndex 本地缓存 + 批量 DB 读取构建预计算缓存。
pub fn failed_sql_dump_count() → usize  :43-45  # 本次运行的 failed_sql 转储计数（spec 004 任务指标采集用）。
pub async fn persist_tubi_relations_from_artifacts(artifacts: &ShapeInstancesData,) → anyhow::Result<usize>  :207-322  # 从本次运行合并后的 `ShapeInstancesData` 一次性持久化 TUBI 关系。
pub async fn pre_cleanup_for_regen(seed_refnos: &[RefnoEnum], whole_dbnum_scope: bool,) → anyhow::Result<()>  :719-724  # 模型重新生成前的预处理清理
pub async fn pre_cleanup_for_regen_versioned(seed_refnos: &[RefnoEnum], hierarchy: &crate::generation_read::HierarchySnapshot,) → anyhow::Result<()>  :726-732
pub async fn save_instance_data_optimize(inst_mgr: &ShapeInstancesData, replace_exist: bool, mesh_results: &HashMap<u64, MeshResult>, mesh_aabb_map: &DashMap<String, Aabb>,) → anyhow::Result<()>  :1023-1032
pub async fn save_instance_data_with_options(inst_mgr: &ShapeInstancesData, replace_exist: bool, mesh_results: &HashMap<u64, MeshResult>, mesh_aabb_map: &DashMap<String, Aabb>, write_inst_relate_aabb: bool,) → anyhow::Result<()>  :1034-1050
pub async fn save_instance_data_with_report(inst_mgr: &ShapeInstancesData, replace_exist: bool, mesh_results: &HashMap<u64, MeshResult>, mesh_aabb_map: &DashMap<String, Aabb>, write_inst_relate_aabb: bool,) → anyhow::Result<SaveInstance...  :1071-1087
pub async fn save_instance_data_with_report_versioned(inst_mgr: &ShapeInstancesData, replace_exist: bool, mesh_results: &HashMap<u64, MeshResult>, mesh_aabb_map: &DashMap<String, Aabb>, write_inst_relate_aabb: bool, precomputed: &InstRelatePrecomputed,) → anyhow::Result<SaveInstance...  :1089-1106
pub fn build_inst_relate_aabb_rows(inst_mgr: &ShapeInstancesData, mesh_results: &HashMap<u64, MeshResult>, mesh_aabb_map: &DashMap<String, Aabb>,) → anyhow::Result<(HashMap<u64...  :2003-2040
pub async fn save_inst_relate_aabb_rows(aabb_map: &HashMap<u64, String>, inst_relate_aabb_rows: &[String], inst_relate_aabb_ids: &[String],) → anyhow::Result<()>  :2097-2152
pub async fn save_tubi_info_batch(tubi_info_map: &DashMap<String, TubiInfoData>,) → anyhow::Result<usize>  :2404-2431  # 批量保存 tubi_info 数据到数据库（INSERT IGNORE 幂等）。
pub async fn persist_negative_relations_from_artifacts(artifacts: &ShapeInstancesData,) → anyhow::Result<usize>  :2437-2544  # 仅使用本次 run 汇总的内存事实补写跨 batch 负关系。
pub async fn reconcile_missing_neg_relate(all_refnos: &[RefnoEnum], candidate_carriers: &[RefnoEnum],) → anyhow::Result<usize>  :2555-2705  # 补建跨阶段缺失的 neg_relate
pub async fn save_instance_data_to_sql_file(inst_mgr: &ShapeInstancesData, replace_exist: bool, writer: &SqlFileWriter, precomputed: &InstRelatePrecomputed, mesh_results: &HashMap<u64, MeshResult>, mesh_aabb_map: &DashMap<String, Aabb>,) → anyhow::Result<()>  :3128-3259
```

### src\generation_read\hierarchy.rs
```
pub struct HierarchyNode  :12-18
pub struct HierarchyQuery  :33-38
pub struct HierarchySnapshot  :41-48
impl HierarchySnapshot  :50-188
  pub async fn load(session: Arc<dyn VersionedReadSession>, dbnums: &[u32],) → GenerationReadResult<Self>  :51-54
  pub async fn load_for_refnos(session: Arc<dyn VersionedReadSession>, seeds: &[RefnoEnum],) → GenerationReadResult<Self>  :66-69
  pub fn from_parts(snapshot_id: u64, elements: Vec<ElementSnapshot>, rows: Vec<HierarchyRow>,) → GenerationReadResult<Self>  :146-150
```

### src\version_management\cli.rs
```
pub fn model_version_command() → Command  :7-119
pub fn repair_missing_meshes_command() → Command  :360-399
pub async fn handle_model_version_command(matches: &ArgMatches, db_option_ext: &DbOptionExt,) → anyhow::Result<bool>  :414-435
pub async fn handle_repair_missing_meshes_command(matches: &ArgMatches, db_option_ext: &DbOptionExt,) → anyhow::Result<bool>  :2048-2108
```

### src\version_management\increment_run.rs
```
pub struct DbnumIncrementRange  :18-22
pub struct IncrementRunOptions  :27-44  # 一次增量运行的全部输入。区间起点语义：从 `from_sesno + 1` 收集到
pub struct IncrementRunResult  :47-54  # 运行结果。`parquet_export` 序列化为 JSON，公共接口不依赖 `gen_model` feature。
pub async fn run_increment(db_option_ext: &DbOptionExt, options: IncrementRunOptions, ensure_model_store: C,) → anyhow::Result<IncrementRun...  :79-99  # 执行一次增量运行。
```

### src\version_management\model_impact.rs
```
pub enum AttributeModelImpact  :16-20  # 属性对模型生成输入的影响。未知属性保守触发，只有明确列出的业务元数据可跳过。
pub enum AttributeModelEffect  :32-45  # 属性改动的「模型影响 effect」——对齐 Core3D `DCHC/EVALAT` 的**作用域路由**。
impl AttributeModelEffect  :47-83
pub fn is_delivery_unit_root_noun(noun: &str) → bool  :10-12
pub fn normalize_attribute_name(raw_name: &str) → String  :85-94
pub fn classify_attribute_model_impact(raw_name: &str) → AttributeModelImpact  :96-103
pub fn classify_attribute_model_effect(raw_name: &str) → AttributeModelEffect  :142-161  # 把一个属性映射到 [`AttributeModelEffect`]（对齐 Core3D DCHC 路由，见枚举文档）。
pub fn attribute_affects_model(raw_name: &str) → bool  :173-234  # 判断单个 E3D/PDMS 属性是否会改变生成器的模型输入。
pub fn field_path_affects_model(path: &str) → bool  :237-247  # PE/ATT 历史 diff 的字段路径是否会影响模型。
```

### src\versioned_db\cata_ref_closure.rs
```
pub struct ReverseClosureLimits  :23-30  # 反查闭包的保护上限。
pub struct ReverseClosureResult  :44-55  # 反查闭包结果（shadow：仅供日志/对账，不消费）。
impl ReverseClosureLimits  :32-40
impl ClosureAccumulator  :66-123
pub fn expand_reverse_closure(seeds: &[RefU64], limits: &ReverseClosureLimits, mut lookup: F,) → ReverseClosureResult where ...  :129-158  # 纯 BFS 反查闭包（无 DB，注入 `lookup` 便于单测）。
pub async fn expand_catalogue_reverse_targets(seeds: &[RefU64], families: Option<&[String]>, limits: &ReverseClosureLimits,) → anyhow::Result<ReverseClosu...  :164-194  # P2 shadow 入口：对「被改的目录定义 seeds」做反查闭包（多跳），使用 `cata_ref_index` 一
```

### src\versioned_db\cata_ref_index.rs
```
pub struct RefEdge  :49-55  # 一条 as-written 语法引用边。`source_refno`/`target_refno` 用规范 `"ref0
pub struct RefIndexState  :108-115  # `cata_ref_index_state` 行：某 dbnum 的 backfill 水位（ready 门 + 对账基
pub struct EdgeDigest  :358-361  # XOR-fold 累加器：对边集做顺序无关的稳定摘要，backfill 与 audit 两侧独立复算一致。
impl RefEdge  :57-83
impl EdgeDigest  :363-389
  pub fn new() → Self  :364-364
  pub fn absorb(&mut self, edge: &RefEdge)  :368-368
  pub fn absorb_all(&mut self, edges: impl IntoIterator<Item = &'a RefEdge>)  :376-376
  pub fn count(&self) → usize  :382-382
  pub fn checksum(&self) → String  :386-386
pub async fn ensure_cata_ref_index_schema() → anyhow::Result<()>  :123-133  # 代码内 ensure（仿 `pe_owner_tree` / `version_commit`）：`pe` 为 SCHE
pub fn extract_ref_edges(source: RefU64, source_dbnum: u32, att: &NamedAttrMap) → Vec<RefEdge>  :144-181  # 从一个元素的属性 map 抽取全部 as-written 引用边（除层级/自身标识轴）。
pub fn build_replace_by_source_sql(source_dbnum: u32, source_refno: &str, edges: &[RefEdge],) → (String, Vec<String>)  :217-228  # 单个 source 的 replace-by-source：返回 (删除语句, 插入语句集)。
pub fn delete_sources_sql(source_dbnum: u32, sources: &[String]) → Option<String>  :231-239  # 批量删除一页 source 的现有出边（backfill 幂等重跑用）。
pub fn insert_edges_sql(edges: &[RefEdge], chunk: usize) → Vec<String>  :242-251  # 分块 INSERT（每块 `chunk` 条）。
pub async fn load_outbound_references(source_dbnum: u32, sources: &[RefU64],) → anyhow::Result<Vec<RefEdge>>  :258-272  # 出边：给定 source 集合读其全部引用边（audit / 维护用）。
pub async fn load_inbound_references(targets: &[RefU64], families: Option<&[String]>, limit: usize,) → anyhow::Result<Vec<RefEdge>>  :276-299  # 入边：反查「谁引用了这些 target」。一跳分页原语（P2 expander 消费）。
pub async fn read_state(dbnum: u32) → anyhow::Result<Option<RefIn...  :305-312
pub async fn write_state(dbnum: u32, ready: bool, row_count: usize, checksum: &str,) → anyhow::Result<()>  :314-329
pub async fn count_index_rows(dbnum: u32) → anyhow::Result<usize>  :341-350  # 该 dbnum 索引里的全部出边行数（含可能的孤儿 source，用于 audit 孤儿检测）。
pub fn edges_equal_ignoring_order(left: &[RefEdge], right: &[RefEdge]) → bool  :392-401  # 同一 source 的边集是否内容一致（audit 差分用；忽略行内顺序）。
```

### src\data_interface\db_meta_manager.rs
```
pub struct DbMetaManager  :14-21  # 数据库元信息管理器
pub struct DbFileInfo  :24-31
impl DbMetaManager  :33-188
  pub fn global() → &'static DbMetaManager  :43-43  # 获取全局单例
  pub fn load(&self, path: impl AsRef<Path>) → Result<()>  :48-48  # 加载 db_meta_info
  pub fn try_load_default(&self) → Result<()>  :125-125  # 尝试从默认路径加载
pub fn db_meta() → &'static DbMetaManager  :360-362  # 便捷函数：获取全局管理器
pub fn get_dbnum(ref0: u32) → Option<u32>  :365-367  # 便捷函数：根据 ref0 获取 dbnum
pub fn ref0s_to_dbnums(ref0s: &[u32]) → Vec<u32>  :370-372  # 将 ref0 列表转换为 dbnum 列表（去重）
pub fn resolve_dbnum_for_refno(refno: aios_core::RefnoEnum) → anyhow::Result<u32>  :454-469  # 从 refno 解析 dbnum（cache-only，依赖 db_meta_info
pub fn generate_desi_db_meta(ignore_manual_dbnum: bool) → anyhow::Result<()>  :472-474  # 生成所有 DESI 类型的 db_meta_info
pub fn generate_all_types_db_meta(ignore_manual_dbnum: bool) → anyhow::Result<()>  :480-485  # 生成全部库类型（DESI/CATA/DICT/SYST/GLB/GLOB）的 db_meta_info
pub fn generate_single_db_meta(target_dbnum: u32) → anyhow::Result<()>  :544-650  # 生成指定 dbnum 的 db_meta_info
```

### src\fast_model\cata_cache_gen.rs
```
pub struct SimpleCataOutcome  :40-44  # model cache 专用的简化 CATA 输出结构
pub struct SimpleBranOutcome  :48-52  # model cache 专用的简化 BRAN 输出结构
pub async fn gen_cata_geos_for_cache(db_option: Arc<DbOptionExt>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, _sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<SimpleCataOu...  :152-294  # model cache 专用：仅生成 CATA 几何体，不收集 tubi 相关信息
pub async fn gen_bran_geos_for_cache(db_option: Arc<DbOptionExt>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, _sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<SimpleBranOu...  :451-603  # model cache 专用：仅生成 BRAN/HANG 几何体，不生成 tubi
pub async fn gen_tubi_for_cache(db_option: Arc<DbOptionExt>, branch_refnos: &[RefnoEnum], sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<BranchTubiOu...  :651-670  # model cache 专用：在所有 BRAN 模型生成完成后，单独遍历 BRAN 生成 tubi
pub async fn gen_tubi_for_cache_with_cache_manager(db_option: Arc<DbOptionExt>, branch_refnos: &[RefnoEnum], sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<BranchTubiOu...  :673-763  # 同 [`gen_tubi_for_cache`]，但复用外部提供的 `InstanceCacheManager`。
```

### src\fast_model\export_model\export_dbnum_instances_parquet.rs
```
pub struct ParquetExportStats  :2374-2385  # Parquet 导出统计信息
pub fn publish_dbnum_latest_manifest(artifact_dir: &Path, latest_root: &Path, dbnum: u32,) → Result<PathBuf>  :49-166
pub async fn query_distinct_dbnums_from_inst_relate() → Result<Vec<u32>>  :1417-1431  # 从 inst_relate 查询所有 distinct dbnum（排序后返回）
pub async fn export_dbnum_instances_parquet(dbnum: u32, output_dir: &Path, db_option: Arc<DbOption>, verbose: bool, target_unit: Option<LengthUnit>, root_refno: Option<RefnoEnum>,) → Result<ParquetExportStats>  :2403-2571
pub async fn export_dbnum_instances_parquet_latest(dbnum: u32, latest_root: &Path, db_option: Arc<DbOption>, verbose: bool, target_unit: Option<LengthUnit>,) → Result<(ParquetExportStats,...  :3301-3346
```

### src\fast_model\export_model\export_dbnum_instances_web.rs
```
pub struct WebExportStats  :34-43
impl NameTable  :108-143
pub async fn export_dbnum_instances_web(dbnum: u32, output_dir: &Path, db_option: Arc<DbOption>, verbose: bool, root_refno: Option<RefnoEnum>, mesh_base_dir: Option<PathBuf>,) → Result<WebExportStats>  :353-519  # 导出 delivery-code 兼容的 V2 格式 instances
```

### src\fast_model\export_model\export_instanced_bundle.rs
```
pub struct InstancedManifest  :33-39  # Manifest 清单文件
pub struct ArchetypeInfo  :43-50  # Archetype 信息
pub struct LodLevelInfo  :54-58  # LOD 级别信息
pub struct InstancesData  :62-65  # 实例数据文件
pub struct InstanceInfo  :82-90  # 单个实例信息
pub struct InstancedBundleExporter  :93-96  # Instanced Bundle 导出器
impl InstancedBundleExporter  :98-254
  pub fn new(db_option: Arc<DbOption>, verbose: bool) → Self  :99-99
  pub async fn export(&self, export_data: &ExportData, output_dir: &Path, mesh_dir: &Path,) → Result<()>  :104-109  # 导出 instanced bundle
pub async fn export_instanced_bundle_for_refnos(refnos: &[RefnoEnum], mesh_dir: &Path, output_dir: &Path, db_option: Arc<DbOption>, verbose: bool,) → Result<super::export_common...  :376-517  # 为指定 refnos 导出 instanced bundle（入口函数）
```

### src\fast_model\export_model\export_prepack_lod.rs
```
pub struct InstancesMeta  :118-131
pub struct InstancesData  :176-189
pub struct ComponentGroup  :207-229
pub struct HierarchyGroup  :235-259
pub struct GeoEntry  :265-271
pub struct TubingInstance  :277-293
pub struct GeometryManifest  :299-307
pub struct GeometryEntry  :313-321
pub struct CompactGeoInstance  :3052-3057
pub struct CompactEntry  :3063-3080
pub struct CompactGroup  :3086-3098
pub enum GeometryType  :166-170
impl NameTable  :1839-1867
impl ColorPalette  :1877-2030
impl HashCollector  :3175-3203
pub async fn export_prepack_lod_for_refnos(refnos: &[RefnoEnum], mesh_dir: &Path, output_dir: &Path, db_option: Arc<DbOption>, include_descendants: bool, filter_nouns: Option<Vec<String>>, verbose: bool, name_config: Option<&super::name_config::NameConfig>, export_all_lods: bool, source_length_unit: LengthUnit, target_length_unit: LengthUnit,) → Result<()>  :337-562
pub async fn export_all_relates_prepack_lod(dbnum: Option<u32>, verbose: bool, output_override: Option<PathBuf>, owner_types: Option<Vec<String>>, name_config: Option<super::name_config::NameConfig>, db_option: Arc<DbOption>, export_all_lods: bool, export_refnos: Option<String>, source_unit: String, target_unit: String,) → Result<()>  :2148-2373
pub async fn export_all_relates_prepack_lod_parquet(dbnum: Option<u32>, verbose: bool, output_override: Option<PathBuf>, owner_types: Option<Vec<String>>, name_config: Option<super::name_config::NameConfig>, db_option: Arc<DbOption>, export_all_lods: bool, export_refnos: Option<String>, source_unit: String, target_unit: String,) → Result<()>  :2463-2660
pub async fn export_trans_aabb_incremental(output_dir: &Path, needed_trans_hashes: &HashSet<String>, needed_aabb_hashes: &HashSet<String>, unit_converter: &UnitConverter, verbose: bool,) → Result<(usize, usize, usize...  :3412-3519
pub async fn export_dbnum_instances_json(dbnum: u32, output_dir: &Path, db_option: std::sync::Arc<DbOption>, verbose: bool, target_unit: Option<LengthUnit>, root_refno: Option<RefnoEnum>, detailed: bool,) → Result<ExportStats>  :3585-3803
pub async fn export_dbnum_instances_json_from_cache(dbnum: u32, output_dir: &Path, cache_dir: &Path, mesh_dir: Option<&Path>, mesh_lod_tag: Option<&str>, verbose: bool, target_unit: Option<LengthUnit>, detailed: bool,) → Result<(ExportStats, usize,...  :4603-4811
pub async fn export_global_trans_aabb_json(output_dir: &Path, target_unit: Option<LengthUnit>, verbose: bool,) → Result<(usize, usize)>  :5937-6125
pub async fn export_instances_json_for_dbnos(dbnos: &[u32], _mesh_dir: &Path, output_dir: &Path, db_option: Arc<DbOption>, _verbose: bool,) → anyhow::Result<()>  :6992-7022
pub async fn export_instances_json_for_refnos_grouped_by_dbno(refnos: &[RefnoEnum], _mesh_dir: &Path, output_dir: &Path, db_option: Arc<DbOption>, verbose: bool,) → anyhow::Result<()>  :7042-7092
pub async fn export_instances_json_for_refnos_grouped_by_dbno_merge(refnos: &[RefnoEnum], mesh_dir: &Path, output_dir: &Path, db_option: Arc<DbOption>, verbose: bool,) → anyhow::Result<()>  :7100-7237
```

### src\fast_model\export_model\model_exporter.rs
```
pub struct CommonExportConfig  :57-85  # 通用导出配置
pub struct ObjExportConfig  :145-148  # OBJ 导出配置
pub struct GlbExportConfig  :160-163  # GLB 导出配置
pub struct GltfExportConfig  :175-178  # glTF 导出配置
pub struct XktExportConfig  :190-208  # XKT 导出配置
pub struct ExportStats  :225-252  # 导出统计信息
pub trait ModelExporter  :21-21  # 模型导出器 Trait
impl CommonExportConfig  :87-100
impl CommonExportConfig  :102-141
  pub fn with_unit_conversion(include_descendants: bool, filter_nouns: Option<Vec<String>>, verbose: bool, source_unit: LengthUnit, target_unit: LengthUnit,) → Self  :104-110  # 创建带单位转换的配置
  pub fn with_unit_conversion_str(include_descendants: bool, filter_nouns: Option<Vec<String>>, verbose: bool, source_unit: &str, target_unit: &str,) → Result<Self, String>  :124-130  # 创建带单位转换字符串的配置
impl ObjExportConfig  :150-156
impl GlbExportConfig  :165-171
impl GltfExportConfig  :180-186
impl XktExportConfig  :210-221
impl ExportStats  :254-286
  pub fn new() → Self  :256-256  # 创建新的统计信息
  pub fn print_summary(&self, format_name: &str)  :261-261  # 打印统计摘要
pub async fn collect_export_refnos(input_refnos: &[RefnoEnum], include_descendants: bool, filter_nouns: Option<&[String]>, verbose: bool,) → Result<Vec<RefnoEnum>>  :300-384  # 共享的辅助函数：收集所有需要导出的 refnos（包括子孙节点）
pub async fn query_geometry_instances(refnos: &[RefnoEnum], enable_holes: bool, verbose: bool,) → Result<Vec<GeomInstQuery>>  :397-403  # 共享的辅助函数：查询几何体实例数据
pub async fn query_geometry_instances_ext(refnos: &[RefnoEnum], enable_holes: bool, include_negative: bool, verbose: bool,) → Result<Vec<GeomInstQuery>>  :417-462  # 共享的辅助函数：查询几何体实例数据（支持负实体）
pub async fn query_geometry_instances_ext_from_cache(refnos: &[RefnoEnum], cache_dir: &Path, enable_holes: bool, include_negative: bool, verbose: bool,) → Result<Vec<GeomInstQuery>>  :469-484  # 缓存路径：从 model/instance_cache 读取几何实例数据，构造与 SurrealDB `query_in
```

### src\fast_model\export_model\post_gen_export.rs
```
pub struct PostGenerationParquetExportReport  :9-15
impl PostGenerationParquetExportReport  :17-37
pub async fn export_parquet_after_generation_if_enabled(db_option_ext: &DbOptionExt, dbnums_hint: Option<Vec<u32>>,) → Result<PostGenerationParque...  :39-62
```

### src\fast_model\export_model\spec_info.rs
```
impl SpecHierSource  :45-61
pub async fn build_spec_info_parquet(dbnum: u32, tree_dir: &Path, output_path: &Path, verbose: bool,) → Result<HashMap<u64, i64>>  :64-212  # 构建 spec_info 并写出 Parquet，返回 refno -> spec_value 映射供导出使用
pub async fn load_or_build_spec_info(dbnum: u32, tree_dir: &Path, output_dir: &Path, verbose: bool,) → Result<HashMap<u64, i64>>  :234-247  # 若 spec_info 文件已存在则加载，否则构建并保存；返回 refno -> spec_value
```

### src\fast_model\gen_model\boolean_task.rs
```
pub struct BooleanTask  :15-22  # 单个布尔运算任务
pub struct CataNegBoolTask  :35-42  # 元件库布尔任务数据（等价于 `CataNegGroup`，但从内存传递）
pub struct CataGeoData  :46-53  # 元件库布尔中单个几何体的数据
pub struct InstNegBoolTask  :57-64  # 实例级布尔任务数据（等价于 `ManiGeoTransQuery`，但从内存传递）
pub struct PosGeoData  :68-73  # 正实体几何数据
pub struct NegEntityData  :77-86  # 负实体数据（一个 carrier 的几何集合）
pub struct NegGeoData  :90-99  # 单个负几何体数据
pub struct BooleanTaskAccumulator  :205-207  # 跨批次布尔任务汇总器。
pub enum BooleanTaskType  :26-31  # 布尔任务类型
impl BooleanTaskAccumulator  :209-247
  pub fn merge_batch(&mut self, batch: &ShapeInstancesData)  :211-211  # 合并单个批次到累积状态
  pub fn build_tasks(&self) → Vec<BooleanTask>  :240-240  # 从已汇总数据构建完整布尔任务
  pub fn into_merged(self) → ShapeInstancesData  :244-244
pub fn semantic_hash_boolean_tasks(tasks: &[BooleanTask]) → String  :103-198  # 对 boolean worker 的完整输入做规范化哈希，供双后端端到端语义对拍。
pub fn extract_cata_neg_tasks(shape_insts: &ShapeInstancesData) → Vec<BooleanTask>  :270-322  # 从 ShapeInstancesData 中提取元件库布尔任务
pub fn extract_inst_neg_tasks(shape_insts: &ShapeInstancesData) → Vec<BooleanTask>  :329-452  # 从 ShapeInstancesData 中提取实例级布尔任务
pub fn build_boolean_tasks(shape_insts: &ShapeInstancesData) → Vec<BooleanTask>  :455-460  # 从 ShapeInstancesData 构建完整的布尔任务集合
```

### src\fast_model\gen_model\cata_model.rs
```
pub struct CateGenOutcome  :136-147
pub struct BranchTubiOutcome  :347-357
pub struct GenOutcome  :359-363
pub struct BranchPrefetchResult  :567-571
pub struct BranchMetaPrefetched  :577-581
pub enum NgmrRemovedType  :113-132
pub fn init_chrome_tracing() → anyhow::Result<()>  :102-108
pub async fn prefetch_tubi_size_and_branch_meta(generation_read: Arc<GenerationReadContext>, all_child_refnos: &[RefnoEnum], branch_refnos: &[RefnoEnum],) → anyhow::Result<BranchPrefet...  :591-773
pub async fn gen_cata_geos(db_option: Arc<DbOptionExt>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :824-853
pub async fn gen_cata_instances(db_option: Arc<DbOptionExt>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<CateGenOutcome>  :857-874
pub async fn gen_cata_instances_versioned(db_option: Arc<DbOptionExt>, generation_read: Arc<GenerationReadContext>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<CateGenOutcome>  :877-904  # Session-bound CATE entry used by the unified executor
pub async fn gen_branch_tubi(db_option: Arc<DbOptionExt>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32)  :908-908
pub async fn gen_branch_tubi_from_db_with_prefetch(db_option: Arc<DbOptionExt>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32)  :926-926
pub async fn gen_branch_tubi_from_db_with_prefetch_versioned(db_option: Arc<DbOptionExt>, generation_read: Arc<GenerationReadContext>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32)  :950-950  # Session-bound BRAN/HANG entry used by the unified executor
pub async fn gen_branch_tubi_from_db(db_option: Arc<DbOptionExt>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32)  :985-985
pub async fn query_ngmr_owner(read: &GenerationReadContext, refno: RefnoEnum, ngmr_geo_refno: RefnoEnum,) → anyhow::Result<Vec<RefnoEnum>>  :5610-5688
pub async fn gen_tubi_from_db(db_option: Arc<DbOptionExt>, branch_refnos: &[RefnoEnum], sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<BranchTubiOu...  :5718-5818
```

### src\fast_model\gen_model\cate_processor.rs
```
pub async fn process_cate_refno_page(ctx: &NounProcessContext, loop_sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → Result<()>  :18-65  # 处理 Cate (元件库) 类型的 refno 页面
```

### src\fast_model\gen_model\cate_single.rs
```
pub async fn gen_cata_single_geoms  :52-58
pub async fn gen_cata_single_geoms_with_session  :60-67
```

### src\fast_model\gen_model\config.rs
```
pub struct Concurrency  :185-185  # 类型安全的并发配置
pub struct BatchSize  :257-257  # 类型安全的批次大小配置
pub struct GenPipelineConfig  :311-332  # GenPipeline的统一配置
impl GenerationContract  :36-93
impl ExecutionTuning  :115-135
impl Concurrency  :187-247
  pub fn new(n: usize) → Result<Self>  :211-211  # 创建新的并发配置
  pub fn default() → Self  :238-238  # 创建默认并发配置
  pub fn get(&self) → usize  :244-244  # 获取并发数值
impl Concurrency  :249-253
impl BatchSize  :259-299
  pub fn new(n: usize) → Result<Self>  :270-270  # 创建新的并发配置
  pub fn default() → Self  :291-291  # 创建默认并发配置
  pub fn get(&self) → usize  :296-296  # 获取并发数值
impl BatchSize  :301-305
impl GenPipelineConfig  :334-494
  pub fn from_db_option(_opt: &DbOption) → Result<Self>  :345-345  # 从 DbOption 创建配置
  pub fn from_db_option_ext(opt: &crate::options::DbOptionExt) → Result<Self>  :353-353  # 从 DbOptionExt 创建配置（推荐）
  pub fn default() → Self  :369-369  # 创建默认并发配置
  pub fn with_concurrency(mut self, concurrency: Concurrency) → Self  :382-382  # 构建器模式：设置并发数
  pub fn with_batch_size(mut self, batch_size: BatchSize) → Self  :388-388  # 构建器模式：设置批次大小
  pub fn with_strict_validation(mut self, strict: bool) → Self  :394-394  # 构建器模式：设置严格验证
  pub fn with_enabled_categories(mut self, categories: Vec<String>) → Self  :400-400  # 构建器模式：设置启用的类别
  pub fn with_excluded_nouns(mut self, nouns: Vec<String>) → Self  :406-406  # 构建器模式：设置排除的 noun
impl GenPipelineConfig  :496-500
```

### src\fast_model\gen_model\context.rs
```
pub struct GenerationReadContext  :12-19  # Noun 处理上下文，包含所有处理过程需要的配置信息
pub struct NounProcessContext  :90-96  # Noun 处理上下文，包含所有处理过程需要的配置信息
impl GenerationReadContext  :21-86
  pub async fn load(session: Arc<dyn VersionedReadSession>) → anyhow::Result<Arc<Self>>  :22-22
  pub async fn load_for_refnos(session: Arc<dyn VersionedReadSession>, refnos: &[aios_core::RefnoEnum],) → anyhow::Result<Arc<Self>>  :28-31
impl NounProcessContext  :98-120
  pub fn new(db_option: Arc<DbOptionExt>, generation_read: Arc<GenerationReadContext>, generation_contract: Arc<GenerationContract>, batch_size: usize, batch_concurrency: usize,) → Self  :105-111  # 创建新的处理上下文
```

### src\fast_model\gen_model\errors.rs
```
pub enum GenPipelineError  :7-43  # GenPipeline特定的错误类型
impl GenPipelineError  :48-90
  pub fn is_fatal(&self) → bool  :50-50  # 是否是致命错误（需要停止处理）
  pub fn is_warning(&self) → bool  :58-58  # 是否是警告级别（可以继续处理）
  pub fn user_message(&self) → String  :68-68  # 获取用户友好的错误消息
```

### src\fast_model\gen_model\hier_view.rs
```
pub struct HierView  :18-20
impl HierView  :22-169
  pub async fn load(dbnums: Vec<u32>) → anyhow::Result<Self>  :24-24  # 加载层级视图。dbnums 为空时从 db_meta 取全部 dbnum。
  pub async fn load_for_refno(refno: RefnoEnum) → anyhow::Result<Self>  :39-39  # 单 refno 场景的便捷入口（自动解析 dbnum）。
  pub fn is_snapshot(&self) → bool  :44-44
  pub fn get_node_meta(&self, refno: RefnoEnum) → Option<TreeNodeMeta>  :63-63
  pub fn get_noun(&self, refno: RefnoEnum) → Option<String>  :68-68
  pub fn contains(&self, refno: RefnoEnum) → bool  :72-72
  pub fn query_children(&self, parent: RefnoEnum) → Vec<RefnoEnum>  :76-76
  pub fn query_children_filtered(&self, parent: RefnoEnum, nouns: &[&str]) → Vec<RefnoEnum>  :93-93
pub async fn collect_children_elements_from_hierarchy(parent: RefnoEnum,) → anyhow::Result<Vec<SPdmsEle...  :378-388  # 收集 parent 的直接子元件（pe_owner 快照）。
pub async fn collect_bran_cate_descendant_elements_from_hierarchy(parent: RefnoEnum,) → anyhow::Result<Vec<SPdmsEle...  :391-416  # 收集 BRAN 下所有 CATE/管件子孙节点（BFS + BRAN_COMPONENT 过滤）。
```

### src\fast_model\gen_model\loop_processor.rs
```
pub async fn process_loop_refno_page(ctx: &NounProcessContext, loop_sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → Result<()>  :27-53  # 处理 Loop Owner 类型的 refno 页面
```

### src\fast_model\gen_model\manifold_bool.rs
```
pub struct BoolWorkerReport  :1701-1711  # 内存任务驱动布尔 worker 执行报告
pub trait BoolResultWriter  :1721-1721
impl DbBoolWriter  :1743-1779
impl SqlBoolWriter  :1785-1789
impl SqlBoolWriter  :1792-1853
pub async fn apply_cata_neg_boolean_manifold(refnos: &[RefnoEnum], replace_exist: bool,) → anyhow::Result<()>  :725-893
pub async fn apply_insts_boolean_manifold(refnos: &[RefnoEnum], replace_exist: bool,) → anyhow::Result<()>  :1612-1697
pub async fn run_bool_worker_from_tasks(tasks: Vec<BooleanTask>, db_option: Arc<aios_core::options::DbOption>, sql_writer: Option<Arc<SqlFileWriter>>,) → anyhow::Result<BoolWorkerRe...  :2331-2337
pub async fn run_bool_worker_from_tasks_versioned(tasks: Vec<BooleanTask>, db_option: Arc<aios_core::options::DbOption>, sql_writer: Option<Arc<SqlFileWriter>>,) → anyhow::Result<BoolWorkerRe...  :2341-2347  # 版本化正式路径：当前 run 的 boolean task 是唯一输入事实，不读取既有模型表
```

### src\fast_model\gen_model\mesh_state.rs
```
pub struct MeshAssetStore  :10-13
impl MeshAssetStore  :15-40
pub fn new  :16-16
pub fn contains  :26-26
pub fn use_file_mesh_state  :42-47
pub fn flush_aabb_cache  :49-51
pub fn mesh_exists  :53-56
pub fn get_cached_or_local_aabb  :58-61
pub fn get_cached_or_local_aabb_in_dir  :63-75
pub fn prime_cached_aabb_for_mesh_ids  :77-91
pub fn mesh_file_exists_in_dir  :93-95
```

### src\fast_model\gen_model\models.rs
```
pub struct DbModelInstRefnos  :21-26  # 一个db生成模型里，汇总的参考号集合
pub enum NounCategory  :10-17  # Noun 分类枚举，用于 GenPipeline 目标类型处理
impl DbModelInstRefnos  :28-59
  pub async fn execute_gen_inst_meshes(&self, db_option_arc: Option<Arc<DbOptionExt>>)  :29-29
```

### src\fast_model\gen_model\neg_query.rs
```
pub fn group_by_dbnum(refnos: &[RefnoEnum], mut resolver: F,) → anyhow::Result<HashMap<u32,...  :15-28
pub fn group_by_dbnum_best_effort(refnos: &[RefnoEnum], mut resolver: F,) → (HashMap<u32, Vec<RefnoEnum...  :35-51  # 按 dbnum 分组，但不会因单个元素解析失败而整体失败。
pub async fn query_descendants_map_by_dbnum(_tree_dir: impl AsRef<Path>, roots: &[RefnoEnum], nouns: &[&str], include_self: bool,) → anyhow::Result<HashMap<Refn...  :59-125  # 按 dbnum 分组查询子孙集合（pe_owner 快照），返回 root -> descendants 映射。
pub async fn query_descendants_map_by_dbnum_dual(tree_dir: impl AsRef<Path>, roots: &[RefnoEnum], nouns: &[&str], include_self: bool,) → anyhow::Result<HashMap<Refn...  :128-135  # 兼容旧 dual 名称。
```

### src\fast_model\gen_model\noun_collection.rs
```
pub struct GenPipelineTargetCollection  :116-123  # GenPipeline下的目标类型聚合结果
impl GenerationTargets  :25-101
impl GenPipelineTargetCollection  :125-274
  pub fn collect(extra_nouns: Option<&[&'static str]>) → Self  :134-134  # 聚合并去重所有 Noun 列表
  pub fn collect_with_config(extra_nouns: Option<&[&'static str]>, config: Option<&super::config::GenPipelineConfig>,) → Self  :141-144  # 聚合并去重 Noun 列表，支持配置过滤
  pub fn get_category(&self, noun: &str) → Option<NounCategory>  :249-249  # 根据 Noun 名称判断其类别
  pub fn total_count(&self) → usize  :262-262  # 获取所有 Noun 的总数
  pub fn get_nouns_by_category(&self, category: NounCategory) → &[&'static str]  :267-267  # 获取指定类别的 Noun 列表
```

### src\fast_model\gen_model\orchestrator.rs
```
pub struct GenModelResult  :90-96
pub struct GenerationRunProvenance  :99-103
impl GenerationRunProvenance  :105-125
  pub fn input_manifest_hash(&self) → &str  :114-114
  pub fn contract_hash(&self) → &str  :118-118
  pub fn target_hash(&self) → &str  :122-122
pub async fn gen_all_geos_data(manual_refnos: Vec<RefnoEnum>, db_option: &DbOptionExt, incr_updates: Option<IncrGeoUpdateLog>,) → Result<GenModelResult>  :145-163  # 主入口函数：生成所有几何体数据
pub async fn gen_all_geos_data_with_read_spec(manual_refnos: Vec<RefnoEnum>, db_option: &DbOptionExt, incr_updates: Option<IncrGeoUpdateLog>, read_spec: GenerationReadSpec,) → Result<GenModelResult>  :171-178  # Opens one generation session from an explicit, immutable rea
pub async fn gen_all_geos_data_with_read_specs(manual_refnos: Vec<RefnoEnum>, db_option: &DbOptionExt, incr_updates: Option<IncrGeoUpdateLog>, read_spec: GenerationReadSpec, cleanup_read_spec: Option<GenerationReadSpec>,) → Result<GenModelResult>  :182-227  # Opens the target generation slice and, when supplied, a sepa
pub async fn gen_all_geos_data_with_session(manual_refnos: Vec<RefnoEnum>, db_option: &DbOptionExt, incr_updates: Option<IncrGeoUpdateLog>, session: Arc<dyn crate::generation_read::VersionedReadSession>,) → Result<GenModelResult>  :229-245
pub async fn update_sqlite_spatial_index_from_cache(db_option: &DbOptionExt, dbnums: &[u32],) → Result<()>  :1173-1261
pub async fn update_sqlite_spatial_index_from_cache(_db_option: &DbOptionExt, _dbnums: &[u32],) → Result<()>  :1264-1269
```

### src\fast_model\gen_model\precheck_coordinator.rs
```
pub struct PrecheckConfig  :37-50  # 预检查配置
pub struct PrecheckStats  :67-82  # 预检查结果统计
pub enum PeTransformPrecheckMode  :25-33  # pe_transform 预检查模式（按需生成）
impl PrecheckConfig  :52-63
pub async fn run_precheck(db_option: &DbOptionExt, config: Option<PrecheckConfig>,) → Result<PrecheckStats>  :332-377  # 执行模型生成前的预检查
```

### src\fast_model\gen_model\prim_processor.rs
```
pub async fn process_prim_refno_page(ctx: &NounProcessContext, sender: flume::Sender<ShapeInstancesData>, refnos: &[RefnoEnum],) → Result<()>  :27-51  # 处理 Prim (基本体) 类型的 refno 页面
```

### src\fast_model\gen_model\processor.rs
```
pub struct NounProcessor  :14-19  # 通用 Noun 处理器
impl NounProcessor  :21-144
  pub fn new(ctx: NounProcessContext, category_name: &'static str, debug_limit_per_noun: Option<usize>,) → Self  :22-26
  pub async fn process_nouns(&self, nouns: &[&'static str], refno_sink: Arc<RwLock<HashSet<RefnoEnum>>>, page_processor: F,) → Result<()> where F: Fn(Vec<...  :44-53  # 处理一批 Nouns，使用提供的页面处理函数
```

### src\fast_model\gen_model\query_compat.rs
```
pub async fn query_type_refnos_by_dbnum(nouns: &[&str], dbnum: u32, has_children: Option<bool>, _include_history: bool,) → anyhow::Result<Vec<RefnoEnum>>  :139-146  # 按类型查询 refno (兼容旧 API)
pub async fn query_multi_children_refnos(refnos: &[RefnoEnum]) → anyhow::Result<Vec<RefnoEnum>>  :168-170
pub async fn query_use_cate_refnos_by_dbnum(cate_names: &[&str], dbnum: u32, _include_history: bool,) → anyhow::Result<Vec<RefnoEnum>>  :181-189  # 查询使用特定 CATE 的 refno (兼容旧 API)
pub async fn get_children_pes(refno: RefnoEnum) → anyhow::Result<Vec<PE>>  :198-200  # 获取子节点的 PE 信息 (兼容旧 API)
pub async fn get_pe(refno: RefnoEnum) → anyhow::Result<Option<PE>>  :203-205  # 获取单个 PE 信息 (兼容旧 API)
pub async fn get_pes_batch(refnos: &[RefnoEnum]) → anyhow::Result<Vec<PE>>  :208-210  # 批量获取 PE 信息 (兼容旧 API)
pub async fn get_children_refnos(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :213-215  # 获取直接子节点 (兼容旧 API)
pub async fn query_visible_geo_descendants(refno: RefnoEnum, include_self: bool, range_str: Option<&str>,) → anyhow::Result<Vec<RefnoEnum>>  :218-230  # 查询可见几何子孙节点（模型生成路径：TreeIndex）
pub async fn query_negative_geo_descendants(refno: RefnoEnum, include_self: bool, range_str: Option<&str>,) → anyhow::Result<Vec<RefnoEnum>>  :233-245  # 查询负实体几何子孙节点（模型生成路径：TreeIndex）
pub async fn query_deep_visible_inst_refnos(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :248-281  # 查询深度可见实例（模型生成路径：pe_owner 快照主路径 / TreeIndex 回退）
pub async fn query_deep_neg_inst_refnos(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :284-286  # 查询深度负实例（模型生成路径：TreeIndex）
pub async fn query_deep_children_refnos(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :289-291  # 查询深层子孙节点 (兼容旧 API)
pub async fn query_filter_deep_children(refno: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :301-306
pub async fn query_ancestor_refnos(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :309-311  # 查询祖先节点 (兼容旧 API)
pub async fn query_filter_ancestors(refno: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :314-333  # 查询特定类型的祖先 (兼容旧 API)
pub async fn query_filter_deep_children_atts(refno: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<NamedAtt...  :336-342  # 查询过滤后的深层子孙属性（模型生成路径：TreeIndex -> SurrealDB）
pub async fn collect_children_filter_ids(refno: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :345-350  # 查询直接子节点（带类型过滤，模型生成路径：TreeIndex）
```

### src\fast_model\gen_model\query_provider.rs
```
impl PeOwnerSnapshotProvider  :58-105
impl PeOwnerSnapshotProvider  :108-194
impl PeOwnerSnapshotProvider  :197-242
impl PeOwnerSnapshotProvider  :245-260
impl PeOwnerSnapshotProvider  :263-307
impl PeOwnerSnapshotProvider  :310-330
pub async fn get_model_query_provider() → anyhow::Result<Arc<dyn Quer...  :37-45  # 获取用于模型生成的查询提供者
pub async fn get_descendants_by_types(root: RefnoEnum, nouns: &[&str], _max_depth: Option<usize>,) → anyhow::Result<Vec<RefnoEnum>>  :368-378
pub async fn get_children_batch(refnos: &[RefnoEnum]) → anyhow::Result<Vec<RefnoEnum>>  :396-402
pub async fn query_by_type(nouns: &[&str], dbnum: i32, has_children: Option<bool>,) → anyhow::Result<Vec<RefnoEnum>>  :420-430  # 查询指定类型的节点
pub async fn query_by_noun_all_db(nouns: &[&str]) → anyhow::Result<Vec<RefnoEnum>>  :452-468  # 按 Noun 全库查询（Full Noun 模式专用）
pub async fn count_noun_all_db(noun: &str) → anyhow::Result<u64>  :471-480  # 统计指定 noun 在全库范围内的实例数量
pub async fn query_noun_page_all_db(noun: &str, start: usize, limit: usize,) → anyhow::Result<Vec<RefnoEnum>>  :483-500  # 根据分页参数获取指定 noun 的 refno 列表
pub async fn get_pes_batch(refnos: &[RefnoEnum]) → anyhow::Result<Vec<PE>>  :519-522  # 批量获取 PE 信息
pub async fn get_pe(refno: RefnoEnum) → anyhow::Result<Option<PE>>  :531-534  # 获取单个 PE 信息
pub async fn get_children(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :543-546  # 获取直接子节点
pub async fn get_ancestors(refno: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :555-558  # 查询所有祖先节点
pub async fn get_ancestors_of_type(refno: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :568-577  # 查询特定类型的祖先
pub async fn get_children_pes(refno: RefnoEnum) → anyhow::Result<Vec<PE>>  :586-589  # 获取子节点的完整 PE 信息
pub async fn get_attmaps_batch(refnos: &[RefnoEnum]) → anyhow::Result<Vec<NamedAtt...  :598-601  # 批量获取属性映射
pub async fn query_multi_descendants(refnos: &[RefnoEnum], nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :620-625
pub async fn query_multi_descendants_with_self(refnos: &[RefnoEnum], nouns: &[&str], include_self: bool,) → anyhow::Result<Vec<RefnoEnum>>  :636-691  # 多起点、多类型的深层子孙查询（支持 include_self 参数）
pub async fn get_provider_name() → String  :700-705  # 获取当前使用的查询提供者名称
pub async fn health_check() → anyhow::Result<bool>  :710-713  # 健康检查
```

### src\fast_model\gen_model\resolve.rs
```
pub async fn get_or_create_scom_info(cata_refno: RefnoEnum) → anyhow::Result<ScomInfo>  :375-470  # 收集SCOM的信息, 暂时慎用缓存
pub async fn resolve_axis_params(refno: RefnoEnum, context: Option<CataContext>,) → anyhow::Result<BTreeMap<i32...  :473-489  # 求解axis的数值
pub async fn resolve_desi_comp(desi_refno: RefnoEnum, tubi_scom: Option<RefnoEnum>, desi_att_opt: Option<&NamedAttrMap>,) → anyhow::Result<CateGeomsInfo>  :728-734  # 求解design component
pub async fn resolve_desi_comp_with_session(read: &GenerationReadContext, desi_refno: RefnoEnum, tubi_scom: Option<RefnoEnum>, desi_att_opt: Option<&NamedAttrMap>,) → anyhow::Result<CateGeomsInfo>  :736-743
```

### src\fast_model\gen_model\session_query.rs
```
pub async fn get_named_attmap(read: &GenerationReadContext, refno: RefnoEnum,) → anyhow::Result<NamedAttrMap>  :8-17
pub async fn get_named_attmaps(read: &GenerationReadContext, refnos: &[RefnoEnum],) → anyhow::Result<BTreeMap<Ref...  :19-36
pub async fn get_world_transforms(read: &GenerationReadContext, refnos: &[RefnoEnum],) → anyhow::Result<BTreeMap<Ref...  :38-55
pub async fn get_world_transform(read: &GenerationReadContext, refno: RefnoEnum,) → anyhow::Result<Transform>  :57-65
pub fn get_children(read: &GenerationReadContext, refno: RefnoEnum) → Vec<RefnoEnum>  :67-69
pub fn get_type_name(read: &GenerationReadContext, refno: RefnoEnum) → anyhow::Result<String>  :71-76
pub fn get_descendants_by_types(read: &GenerationReadContext, root: RefnoEnum, nouns: &[&str], max_depth: Option<usize>, include_self: bool,) → anyhow::Result<Vec<RefnoEnum>>  :78-96
pub fn get_multi_descendants_by_types(read: &GenerationReadContext, roots: &[RefnoEnum], nouns: &[&str], include_self: bool,) → anyhow::Result<Vec<RefnoEnum>>  :98-115
pub fn get_ancestors_by_types(read: &GenerationReadContext, start: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :117-140
pub async fn get_descendant_attmaps(read: &GenerationReadContext, root: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<NamedAtt...  :142-153
pub async fn find_reference_sources(read: &GenerationReadContext, target: RefnoEnum, labels: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :155-174
pub async fn follow_reference_path(read: &GenerationReadContext, start: RefnoEnum, labels: &[&str],) → anyhow::Result<Option<Refno...  :176-198
pub async fn first_outbound_reference(read: &GenerationReadContext, start: RefnoEnum, labels: &[&str],) → anyhow::Result<Option<Refno...  :200-215
pub async fn get_pos_neg_map(read: &GenerationReadContext, roots: &[RefnoEnum],) → anyhow::Result<HashMap<Refn...  :217-247
pub fn dbnums(read: &GenerationReadContext) → BTreeSet<u32>  :249-251
```

### src\fast_model\gen_model\transform_cache.rs
```
pub struct TransformCacheManager  :49-54  # 模型生成阶段的 transform 缓存：
impl TransformCacheManager  :56-217
  pub fn new() → Self  :57-57
  pub fn get_world_transform(&self, dbnum: u32, refno: RefnoEnum) → Option<Transform>  :66-66
  pub fn get_local_transform(&self, dbnum: u32, refno: RefnoEnum) → Option<Transform>  :70-70
  pub fn remove(&self, dbnum: u32, refno: RefnoEnum)  :74-74
  pub fn insert_world_transform(&self, dbnum: u32, refno: RefnoEnum, world: Transform)  :80-80
  pub fn insert_local_transform(&self, dbnum: u32, refno: RefnoEnum, local: Transform)  :84-84
  pub fn is_dbnum_loaded(&self, dbnum: u32) → bool  :88-88
  pub fn load_dbnum_snapshot(&self, dbnum: u32, snapshot: LoadedTransformDbnum)  :92-92
pub fn init_global_transform_cache()  :252-254
pub fn prime_global_transform_cache_from_pe_entries(entries: &[PeTransformEntry]) → usize  :260-301  # 用刚刷新出的 pe_transform entries 预热模型生成阶段的内存 transform_cache。
pub fn clear_global_transform_cache() → usize  :315-323  # 清空全局 transform 缓存（分批生成时在批次间调用）。
pub fn clear_global_transform_cache_for_refnos(refnos: &[RefnoEnum]) → usize  :326-331  # 按 refno 清理全局 transform 缓存（用于分批任务隔离）。
pub fn pin_global_transform_cache_for_refnos(refnos: &[RefnoEnum]) → usize  :334-339  # 为一组 refno 增加全局 transform 缓存租约（pin）。
pub fn release_global_transform_cache_for_refnos(refnos: &[RefnoEnum]) → usize  :342-347  # 释放一组 refno 的全局 transform 缓存租约（不清理条目）。
pub async fn get_world_transform_cache_first(db_option: Option<&DbOptionExt>, refno: RefnoEnum,) → anyhow::Result<Option<Trans...  :483-523  # 模型生成专用：优先从内存/rkyv transform_cache 读取 world_transform；
pub async fn get_local_transform_cache_first(db_option: Option<&DbOptionExt>, refno: RefnoEnum,) → anyhow::Result<Option<Trans...  :527-569  # 模型生成专用：优先从内存/rkyv transform_cache 读取 local_transform；
pub async fn get_world_transforms_cache_only_batch(db_option: &DbOptionExt, refnos: &[RefnoEnum],) → anyhow::Result<HashMap<Refn...  :573-626  # strict cache-only：只从内存/rkyv transform_cache 读取 world_transfo
pub async fn get_local_transforms_cache_only_batch(db_option: &DbOptionExt, refnos: &[RefnoEnum],) → anyhow::Result<HashMap<Refn...  :629-682  # strict cache-only：只从内存/rkyv transform_cache 读取 local_transfo
pub async fn get_world_transform_cache_only(db_option: &DbOptionExt, refno: RefnoEnum,) → anyhow::Result<Transform>  :685-693  # strict cache-only：读取单个 world_transform；miss 直接 Err。
pub async fn get_local_transform_cache_only(db_option: &DbOptionExt, refno: RefnoEnum,) → anyhow::Result<Transform>  :696-704  # strict cache-only：读取单个 local_transform；miss 直接 Err。
pub async fn ensure_world_transforms_present(db_option: &DbOptionExt, refnos: &[RefnoEnum],) → anyhow::Result<()>  :707-713  # strict cache-only：确保给定 refnos 的 world transform 均已存在于缓存；缺失直接
pub async fn ensure_local_transforms_present(db_option: &DbOptionExt, refnos: &[RefnoEnum],) → anyhow::Result<()>  :716-722  # strict cache-only：确保给定 refnos 的 local transform 均已存在于缓存；缺失直接
pub async fn get_world_transforms_cache_first_batch(db_option: Option<&DbOptionExt>, refnos: &[RefnoEnum],) → anyhow::Result<HashMap<Refn...  :730-808  # 批量版 cache-first world_transform 获取：
```

### src\fast_model\gen_model\utilities.rs
```
pub fn is_e3d_debug_enabled() → bool  :22-31  # 检查是否启用 E3D 调试模式
pub fn is_e3d_info_enabled() → bool  :35-44  # 检查是否启用 E3D info 模式
pub fn is_e3d_trace_enabled() → bool  :48-57  # 检查是否启用 E3D trace 模式
pub async fn query_tubi_size(refno: RefnoEnum, tubi_cat_ref: RefnoEnum, is_hang: bool,) → Result<TubiSize>  :62-91  # 查询 Tubi 尺寸
pub async fn query_tubi_size_from_session(read: &GenerationReadContext, tubi_cat_ref: RefnoEnum, is_hang: bool,) → Result<TubiSize>  :93-110
pub async fn build_cata_hash_map_from_tree_by_dbnum(dbnum: u32, refnos: &[RefnoEnum],) → Result<DashMap<String, Cata...  :291-299  # 基于 tree 文件（按 dbnum）构建 cata_hash 分组
pub async fn build_cata_hash_map_from_tree(refnos: &[RefnoEnum],) → Result<DashMap<String, Cata...  :323-384  # 基于 tree 文件（自动按 dbnum 分组）构建 cata_hash 分组
pub async fn build_cata_hash_map_from_session(read: &GenerationReadContext, refnos: &[RefnoEnum],) → Result<DashMap<String, Cata...  :387-418  # 基于固定版本会话构建 cata_hash 分组：roots 自身 + 直接 children。
```

### src\fast_model\gen_model\write_pipeline.rs
```
impl BatchStageJoiner  :73-126
impl WritePipelineReport  :614-618
impl ModelWritePipeline  :639-760
```

### src\fast_model\mod.rs
```
pub struct CaptureConfig  :128-136
pub struct AabbCacheFileV1  :198-201
pub struct AabbCacheEntryV1  :204-208
impl CaptureConfig  :138-162
  pub fn new(output_dir: PathBuf, width: u32, height: u32, include_descendants: bool, views: u8, baseline_dir: Option<PathBuf>, diff_dir: Option<PathBuf>,) → Self  :139-147
pub fn set_capture_config(config: Option<CaptureConfig>)  :166-169
pub fn get_capture_config() → Option<CaptureConfig>  :171-173
pub fn save_aabb_cache_to_disk()  :211-274  # 将 `EXIST_MESH_GEO_HASHES` 中有效的 AABB 持久化到 `meshes/aabb_cache
pub fn preload_mesh_cache()  :317-327  # 从 `aabb_cache
pub fn set_debug_model_errors_only(enabled: bool)  :344-346
pub fn is_debug_model_errors_only() → bool  :350-352
pub fn is_error_message_heuristic(message: &str) → bool  :362-381  # 启发式判断消息是否包含错误信息（用于 smart_debug 过滤）。
```

### src\fast_model\room_model.rs
```
pub struct RoomBuildStats  :1731-1745
pub struct RoomComputeOptions  :1873-1887
pub struct CoarseAabbDiagnostic  :3344-3350  # 粗算（AABB 相交）诊断结果：用于验证 SQLite RTree 粗筛是否正确。
pub struct IncrementalUpdateResult  :6084-6093
impl SpatialIndexScope  :114-122
impl SpatialIndexSpecResolver  :384-438
impl SpatialIndexSpecResolver  :452-460
impl RoomComputeOptions  :1889-1907
impl RoomComputeOptions  :1909-1936
  pub fn with_prebuilt_spatial_index(mut self) → Self  :1910-1910
  pub fn refresh_spatial_index_enabled(&self) → bool  :1915-1915
  pub fn with_surreal_query(mut self) → Self  :1919-1919
  pub fn query_from_cache_enabled(&self) → bool  :1924-1924
  pub fn with_preload_panel_meshes(mut self, enabled: bool) → Self  :1928-1928
  pub fn preload_enabled(&self) → bool  :1933-1933
impl Floor2dConfig  :1956-1966
impl CacheMetrics  :2029-2079
impl PanelProcessOutcome  :3207-3235
impl ComputedRoomRelations  :3252-3274
pub async fn refresh_sqlite_spatial_index_from_inst_relate_aabb(db_nums: Option<&[u32]>, refno_root: Option<RefnoEnum>,) → anyhow::Result<usize>  :1392-1522
pub async fn build_room_relations(db_option: &DbOption, db_nums: Option<&[u32]>, refno_root: Option<RefnoEnum>,) → anyhow::Result<RoomBuildStats>  :2144-2153
pub async fn build_room_relations_with_model_generation(db_option: &DbOption, db_nums: Option<&[u32]>, refno_root: Option<RefnoEnum>,) → anyhow::Result<RoomBuildStats>  :2156-2173
pub async fn build_room_relations_with_overrides(db_option: &DbOption, db_nums: Option<&[u32]>, refno_root: Option<RefnoEnum>, room_keywords_override: Option<&[String]>, force_rebuild: bool,) → anyhow::Result<RoomBuildStats>  :2176-2194
pub async fn build_room_relations_with_cancel(db_option: &DbOption, db_nums: Option<&[u32]>, refno_root: Option<RefnoEnum>, cancel_token: Option<CancellationToken>, progress_callback: Option<Box<dyn Fn(f32, &str) → anyhow::Result<RoomBuildStats>  :2200-2223
pub async fn build_room_panels_relate_for_query(room_key_word: &Vec<String>,) → anyhow::Result<Vec<(RefnoEn...  :2661-2673
```

### src\fast_model\utils.rs
```
pub async fn ensure_surreal_init() → anyhow::Result<()>  :17-41  # 确保 SurrealDB 连接已初始化（幂等，仅首次真正执行 `init_surreal`）。
pub async fn ensure_inst_relate_relation_schema()  :44-76  # 确保 inst_relate 以“关系表”方式工作：in=pe，out=inst_info。
pub async fn save_aabb_to_surreal(aabb_map: &DashMap<String, Aabb>)  :78-82
pub async fn save_aabb_to_surreal_checked(aabb_map: &DashMap<String, Aabb>) → anyhow::Result<()>  :84-112
pub async fn save_inst_relate_bool(refno: RefnoEnum, mesh_id: Option<&str>, status: &str, source: &str,) → anyhow::Result<()>  :117-141  # 保存布尔结果状态
pub async fn save_inst_relate_cata_bool(refno: RefnoEnum, mesh_id: Option<&str>, status: &str, source: &str,)  :144-175  # 保存 catalog 级布尔结果状态（与实例级布尔分表，避免互相覆盖）
pub async fn save_inst_relate_aabb(inst_aabb_map: &DashMap<RefnoEnum, String>, _source: &str)  :231-235  # 批量保存实例 AABB 到普通表 inst_relate_aabb（原始几何 AABB）
pub async fn save_inst_relate_booled_aabb(inst_aabb_map: &DashMap<RefnoEnum, String>, _source: &str,) → anyhow::Result<()>  :238-243  # 批量保存布尔运算后的 AABB 到普通表 inst_relate_booled_aabb
pub async fn save_pts_to_surreal(vec3_map: &DashMap<u64, String>)  :245-249
pub async fn save_pts_to_surreal_checked(vec3_map: &DashMap<u64, String>) → anyhow::Result<()>  :251-270
pub async fn save_transforms_to_surreal(trans_map: &HashMap<u64, String>) → anyhow::Result<()>  :272-292
```

### src\generation_read\catalog.rs
```
pub struct CatalogResolverConfig  :12-20
pub struct CatalogClosure  :49-56
pub struct CatalogResolver  :58-62
impl CatalogResolverConfig  :22-46
impl CatalogResolver  :64-205
  pub fn new(session: Arc<dyn VersionedReadSession>, config: CatalogResolverConfig) → Self  :65-65
  pub fn with_preloaded_nodes(session: Arc<dyn VersionedReadSession>, config: CatalogResolverConfig, nodes: Arc<BTreeMap<RefnoEnum, CatalogNode>>,) → Self  :73-77
  pub fn config(&self) → &CatalogResolverConfig  :85-85
  pub async fn resolve(&self, seeds: &[RefnoEnum]) → GenerationReadResult<Catalo...  :89-89
```

### src\generation_read\error.rs
```
pub enum GenerationReadError  :7-69
```

### src\generation_read\factory.rs
```
pub async fn open_generation_read_session(options: &DbOptionExt,) → GenerationReadResult<Arc<dy...  :17-22  # specs/027（ADR-0008）：生成读取唯一后端 = Surreal 主库。
pub async fn open_generation_read_session_with_spec(options: &DbOptionExt, spec: &GenerationReadSpec,) → GenerationReadResult<Arc<dy...  :26-45  # 按一次生成 run 的不可变读取契约打开 session。
pub async fn resolve_anchored_generation_read_spec(options: &DbOptionExt,) → GenerationReadResult<Genera...  :85-133  # Resolve the one immutable main-table read instant for a gene
pub async fn resolve_cleanup_read_spec(dbnum: u32, model_generation_watermark: u32,) → GenerationReadResult<Option...  :138-165  # Resolve the old hierarchy slice represented by the currently
pub async fn resolve_input_version_manifest(options: &DbOptionExt,) → GenerationReadResult<InputV...  :199-273  # 观测构造输入版本清单：`dbnum → Committed Watermark`。
```

### src\generation_read\surreal.rs
```
pub struct SurrealVersionedReadBackend  :30-32  # Surreal main-table generation reader
pub struct SurrealVersionedReadSession  :40-45
impl SurrealVersionedReadBackend  :34-38
  pub fn new(read_at: Option<String>) → Self  :35-35
impl SurrealVersionedReadBackend  :48-65
impl SurrealVersionedReadSession  :67-82
impl SurrealVersionedReadSession  :85-130
impl SurrealVersionedReadSession  :133-207
impl SurrealVersionedReadSession  :210-248
impl SurrealVersionedReadSession  :251-309
impl SurrealVersionedReadSession  :312-379
impl SurrealVersionedReadSession  :381-518
```

### src\generation_read\types.rs
```
pub struct GenerationReadSpec  :50-54  # Immutable input-read contract shared by every query in one g
pub struct DataVersion  :137-141
pub struct InputVersionManifest  :144-149
pub struct ElementSnapshot  :248-257
pub struct AttributeSet  :325-330
pub struct AttributeReference  :447-453
pub struct HierarchyRow  :456-461
pub struct CatalogNode  :464-472
pub struct TransformSnapshot  :475-480
pub struct ElementQuery  :483-487
pub struct BatchLookup  :490-493
pub struct SessionMetricsSnapshot  :534-539
pub enum GenerationReadBackendKind  :14-16
pub enum GenerationReadMode  :30-33  # Controls whether one generation run observes the current dat
pub enum AttributeValue  :261-277
impl GenerationReadBackendKind  :18-24
  pub fn as_str(self) → &'static str  :19-19
impl GenerationReadMode  :35-42
  pub fn as_str(self) → &'static str  :36-36
impl GenerationReadSpec  :56-134
  pub fn live() → Self  :59-59  # Read the current state
  pub fn at(read_at: impl Into<String>, observed_watermarks: BTreeMap<u32, u32>,) → GenerationReadResult<Self>  :68-71  # Pin every read in the run to the same data-anchor timestamp
  pub fn mode(&self) → GenerationReadMode  :81-81
  pub fn read_at(&self) → Option<&str>  :85-85
  pub fn observed_watermarks(&self) → &BTreeMap<u32, u32>  :89-89
```

### src\init_project.rs
```
pub fn resolve_target_dbnums(cli_dbnums: Option<Vec<u32>>, discovered_dbnums: Vec<u32>,) → Result<Vec<u32>>  :10-48
pub async fn run_init_project_mode(db_option_ext: DbOptionExt, cli_dbnums: Option<Vec<u32>>,) → Result<()>  :50-97
```

### src\lib.rs
```
pub async fn build_room_relations(_db_option: &aios_core::options::DbOption, _db_nums: Option<&[u32]>, _refno_root: Option<aios_core::RefnoEnum>,) → anyhow::Result<()>  :32-42
pub async fn run_cli(db_option_ext: options::DbOptionExt) → anyhow::Result<()>  :320-523
pub fn init_logging(enable_log: bool)  :574-648
pub async fn run_app(option: Option<DbOptionExt>) → anyhow::Result<()>  :652-696
```

### src\options.rs
```
pub struct DbOptionExt  :330-481  # 扩展DbOption，添加异地部署相关的配置
pub enum MeshFormat  :150-157  # 生成的网格模型格式
pub enum ModelWriterMode  :217-222  # 模型生成结果写入后端。
pub enum TransformWriteBackend  :246-253  # pe_transform 刷新结果写入后端。
pub enum TransformReadBackend  :282-288  # pe_transform 读取/对比后端。
pub enum BooleanPipelineMode  :315-320  # 布尔运算管线模式
impl MeshFormat  :159-163
impl ModelWriterMode  :224-228
impl ModelWriterMode  :230-241
  pub fn as_str(&self) → &'static str  :231-231
  pub fn writes_to_surreal(&self) → bool  :238-238
impl TransformWriteBackend  :255-259
impl TransformWriteBackend  :261-277
  pub fn as_str(&self) → &'static str  :262-262
  pub fn writes_to_surreal(&self) → bool  :270-270
  pub fn writes_to_parquet(&self) → bool  :274-274
impl TransformReadBackend  :290-294
impl TransformReadBackend  :296-310
  pub fn as_str(&self) → &'static str  :297-297
  pub fn needs_parquet_feature(&self) → bool  :307-307
impl BooleanPipelineMode  :322-326
impl DbOptionExt  :483-489
impl DbOptionExt  :491-495
impl DbOptionExt  :497-657
  pub fn get_gen_pipeline_concurrency(&self) → usize  :500-500  # 获取 GenPipeline 实际并发数
```

### src\parse_sidecar.rs
```
pub struct ParseSidecarOptions  :43-52
pub struct ParsePreviewRequest  :90-111
pub struct ProjectScanRequest  :114-116
pub struct MdbCandidatesRequest  :120-127  # MBD 候选发现请求：工程组成与 preview 请求同构，便于 UI 复用站点表单状态。
pub struct DbFileResolveRequest  :130-133
pub struct DbFileResolveResponse  :136-139
pub struct DbIndexRebuildRequest  :142-149
pub struct DbIndexRoot  :152-155
pub struct DbIndexRebuildSummary  :158-165
pub struct RunCliJobRequest  :168-177
pub struct RunCliJobResponse  :180-183
pub struct SubmitCliJobResponse  :186-188
pub struct SidecarJobRecord  :191-202
pub struct SidecarSiteProject  :218-228
pub struct ScannedProject  :231-239
pub struct ScannedDbnumConflict  :242-245
pub struct ScanProjectsResult  :248-253
pub struct ManagedSiteParsePlan  :256-270
pub struct ParsePlanFact  :273-281
pub enum ProjectRole  :206-209
pub enum ManagedSiteParsePlanMode  :284-290
impl ProjectRole  :211-215
pub async fn run_parse_sidecar(options: ParseSidecarOptions) → Result<()>  :292-339
```

### src\pe_transform_refresh.rs
```
pub async fn refresh_pe_transform_for_dbnums_compat(dbnums: &[u32]) → Result<usize>  :20-23
pub async fn pe_transform_covers_dbnum(dbnum: u32) → Result<bool>  :35-47  # 探测 pe_transform 是否已覆盖指定 dbnum（整库 count 比较）。
pub async fn pe_transform_covers_instances_for_dbnum(dbnum: u32) → Result<bool>  :59-76  # 探测 pe_transform 是否已覆盖指定 dbnum 的**实例**（inst_relate 行）。
pub async fn refresh_pe_transform_for_dbnums(dbnums: &[u32], db_option: &DbOptionExt,) → Result<usize>  :94-227
pub async fn refresh_pe_transform_for_root_refnos_compat(root_refnos: &[RefnoEnum],) → Result<usize>  :269-274
pub async fn invalidate_pe_transform_for_root_refnos(root_refnos: &[RefnoEnum]) → Result<usize>  :280-317  # 使 roots 及其子孙的 pe_transform 与内存 transform_cache 失效。
pub async fn refresh_pe_transform_for_root_refnos(root_refnos: &[RefnoEnum], db_option: &DbOptionExt,) → Result<usize>  :358-493
```

### src\pe_transform_store.rs
```
pub struct BackendCompareStats  :124-131
pub async fn save_entries_with_backend(db_option: &DbOptionExt, entries: &[PeTransformEntry],) → Result<()>  :10-32
pub async fn load_entries_with_backend(db_option: &DbOptionExt, backend: TransformReadBackend, refnos: &[RefnoEnum],) → Result<Vec<PeTransformEntry>>  :34-51
pub async fn clear_pe_transform_for_dbnums(dbnums: &[u32]) → Result<usize>  :53-93
pub async fn clear_pe_transform_for_refnos(refnos: &[RefnoEnum]) → Result<usize>  :97-121  # 按 refno 列表删除 pe_transform 行（不存在的 key 忽略）。
pub async fn compare_backends_for_dbnums(db_option: &DbOptionExt, dbnums: &[u32],) → Result<Vec<BackendCompareSt...  :133-203
```

### src\scene_tree\init.rs
```
pub struct SceneTreeInitResult  :38-42  # 初始化结果
pub async fn init_scene_tree(mdb_name: &str, force_rebuild: bool) → Result<SceneTreeInitResult>  :69-107  # 初始化 Scene Tree（从 WORLD 开始）
pub async fn init_scene_tree_from_root(root_refno: RefnoEnum, force_rebuild: bool,) → Result<SceneTreeInitResult>  :112-156  # 初始化 Scene Tree（从指定 root refno 开始构建子树）
pub async fn init_scene_tree_by_dbno(dbnum: u32, force_rebuild: bool,) → Result<SceneTreeInitResult>  :163-210  # 初始化 Scene Tree（按 dbnum，WORLD 固定为 `${dbnum}_0`）
```

### src\version_management\mod.rs
```
pub struct SetStatusData  :21-30
pub struct RefnoStatusDifference  :33-49
```

### src\version_management\model_gen_catchup.rs
```
pub struct ModelGenCatchUpOptions  :15-19
pub struct ModelGenCatchUpResult  :22-35
pub async fn catch_up_model_generation(db_option_ext: &DbOptionExt, dbnum: u32, options: ModelGenCatchUpOptions,) → anyhow::Result<ModelGenCatc...  :37-48
```

### src\version_management\model_generation_run.rs
```
pub struct ModelGenerationRunWatermark  :88-92  # Input and pre-run model watermarks observed for one database
pub struct ModelGenerationAnchorSnapshot  :99-103  # Snapshot of a model anchor before or after a run
pub struct ModelGenerationRunStarted  :106-126
pub struct ModelGenerationRunTerminal  :129-142
pub struct AppendModelGenerationRunEventOutcome  :145-150
pub struct RecordedModelGenerationRunStarted  :153-156
pub struct RecordedModelGenerationRunTerminal  :159-162
pub struct ModelGenerationRunRecord  :175-178
pub enum ModelGenerationRunKind  :26-32
pub enum ModelGenerationRunEventType  :48-51
pub enum ModelGenerationRunTerminalResult  :72-75
pub enum ModelGenerationRunState  :166-172
pub enum ModelGenerationRunRepositoryError  :191-215
impl ModelGenerationRunKind  :34-44
impl ModelGenerationRunEventType  :53-68
impl ModelGenerationRunTerminalResult  :77-84
impl ModelGenerationRunRecord  :180-188
  pub fn state(&self) → ModelGenerationRunState  :181-181
pub async fn ensure_model_generation_run_schema() → ModelGenerationRunRepositor...  :230-279  # Define the immutable event table and its query indexes
pub async fn append_started(event: ModelGenerationRunStarted,) → ModelGenerationRunRepositor...  :282-341  # Append the immutable `started` event for a run
pub async fn append_terminal(event: ModelGenerationRunTerminal,) → ModelGenerationRunRepositor...  :346-404  # Append the single immutable terminal event for a run
pub async fn load_model_generation_run(run_id: &str,) → ModelGenerationRunRepositor...  :407-427  # Load both events for one run
pub async fn list_abandoned_runs() → ModelGenerationRunRepositor...  :433-461  # Return started events which have no terminal event
```

### src\version_management\project_mutation_lock.rs
```
pub struct ProjectMutationLock  :23-26  # 进程级项目写入互斥锁。文件保留用于诊断，锁本身由 OS advisory lock 保证。
impl ProjectMutationLock  :33-99
  pub fn acquire(db_option: &DbOptionExt, command: impl Into<String>) → Result<Self>  :34-34
  pub fn acquire_for_current_command(db_option: &DbOptionExt) → Result<Self>  :39-39
  pub fn path(&self) → &Path  :92-92
impl ProjectMutationLock  :101-111
pub fn lock_path(db_option: &DbOptionExt) → PathBuf  :113-121
pub fn is_mutation_contention_error(message: &str) → bool  :128-134  # 判断错误信息是否代表“另有写者/提交正在进行”的正常并发让路（而非真实故障）。
```

### src\version_management\watch_incremental.rs
```
pub struct WatchIncrementalOptions  :15-26
impl WatchIncrementalOptions  :28-42
pub async fn run_watch_incremental(db_option_ext: &DbOptionExt, options: WatchIncrementalOptions,) → anyhow::Result<()>  :47-61  # 运行统一增量轮询。
pub fn print_incremental_sesno_summary(result: &IncrementRunResult)  :382-458
```

### src\versioned_db\database.rs
```
pub enum SenderJsonsData  :62-74
impl ParseProgressHeartbeat  :188-204
impl ParseProgressHeartbeat  :206-210
pub async fn ensure_sesno_version_anchor_schema() → anyhow::Result<()>  :556-582  # 确保 sesno_version_anchor 锚点表 schema 存在（specs/022 T008）。
pub async fn create_project_database(project: &str, url: &str) → anyhow::Result<()>  :732-740  # 初始化project database
pub async fn create_info_database(db_option: &DbOption) → anyhow::Result<()>  :744-805  # 初始化 info 库和表
pub async fn sync_pdms_with_callback(db_option: &DbOption, mut progress_callback: Option<F>,) → anyhow::Result<()> where F:...  :808-970  # 带进度回调的同步pdms数据到数据库
pub async fn sync_pdms(db_option: &DbOption) → anyhow::Result<()>  :988-1134  # 初始化同步pdms数据到数据
pub async fn define_dbnum_event() → anyhow::Result<()>  :1140-1143
pub async fn define_dbnum_event() → anyhow::Result<()>  :1149-1151
pub async fn define_dbnum_event_array_id() → anyhow::Result<()>  :1155-1186  # 定义dbnum_info_table的更新事件, pe 的id 为array的情况
pub async fn define_dbnum_event_array_id() → anyhow::Result<()>  :1189-1191  # 定义dbnum_info_table的更新事件, pe 的id 为array的情况
pub async fn execute_sql(conn: &Pool<MySql>, sql: &str) → bool  :1194-1213
pub async fn sync_total_async_threaded_with_callback(db_option: &DbOption, project: &str, cur_dbno_set: Arc<DashSet<u32>>, db_types: &[&str], proj_progress_chunk: usize, progress_callback: &mut Option<F>, current_project: usize, total_projects: usize,) → anyhow::Result<()> where F:...  :1216-1363  # 带进度回调的多线程同步数据
pub async fn sync_total_async_threaded(db_option: &DbOption, project: &str, cur_dbno_set: Arc<DashSet<u32>>, db_types: &[&str], proj_progress_chunk: usize,) → anyhow::Result<()>  :2022-2154  # 多线程同步数据，包括增量同步
pub async fn parse_single_db_file(db_option: &DbOption, project_name: &str, file_path: &str, target_dbnum: u32,) → anyhow::Result<()>  :3082-3249  # 解析单个 db 文件并生成 indextree
```

### src\versioned_db\model_gen_debt.rs
```
pub struct ModelGenDebtRecord  :13-28
pub struct ModelGenDebtWriteOutcome  :31-36
pub struct ModelGenDebtBucketCounts  :39-46
pub struct ModelGenDebtRange  :83-88
pub struct ModelGenDebtGap  :91-96
pub struct ModelGenDebtCoverage  :99-119
impl ModelGenDebtBucketCounts  :48-80
pub async fn ensure_model_gen_debt_schema() → anyhow::Result<()>  :173-196
pub async fn write_model_gen_debt(dbnum: u32, from_sesno: u32, to_sesno: u32, commit_fingerprint: &str, log: &IncrGeoUpdateLog,) → anyhow::Result<ModelGenDebt...  :198-250
pub fn debt_upsert_sql(dbnum: u32, from_sesno: u32, to_sesno: u32, commit_fingerprint: &str, log: &IncrGeoUpdateLog,) → String  :261-289  # 生成幂等的 `model_gen_debt` UPSERT 语句，供增量 `commit_version` 的 appl
pub async fn model_generation_watermark(dbnum: u32) → anyhow::Result<u32>  :291-303
pub async fn list_model_gen_candidate_dbnums() → anyhow::Result<Vec<u32>>  :305-325
pub async fn reconcile_model_gen_debt_covered_by_watermark(dbnum: u32, model_generation_watermark: u32,) → anyhow::Result<()>  :327-342
pub async fn analyze_model_gen_debt(dbnum: u32) → anyhow::Result<ModelGenDebt...  :366-446
pub async fn finalize_model_generation(dbnum: u32, target_sesno: u32, note: &str,) → anyhow::Result<super::versi...  :448-488
```

### src\versioned_db\model_unit_commit.rs
```
pub struct ModelUnitCommit  :52-66
pub struct ModelUnitCommitOutcome  :169-172
pub enum ModelUnitImpactKind  :16-22
impl ModelUnitImpactKind  :24-34
impl ModelUnitImpactKind  :36-49
impl ModelUnitCommit  :68-166
  pub fn manifest_url(&self) → Option<String>  :69-69
  pub fn validate(&self) → anyhow::Result<()>  :79-79
pub async fn ensure_model_unit_commit_schema() → anyhow::Result<()>  :180-216
pub async fn commit_model_unit(commit: ModelUnitCommit) → anyhow::Result<ModelUnitCom...  :218-325
pub async fn model_unit_commit(dbnum: u32, unit_refno: &str, sesno: u32,) → anyhow::Result<Option<Model...  :327-339
pub async fn latest_model_unit_commit(dbnum: u32, unit_refno: &str,) → anyhow::Result<Option<Model...  :341-365
pub async fn list_model_unit_commits(dbnum: u32, unit_refno: &str,) → anyhow::Result<Vec<ModelUni...  :367-385
```

### src\versioned_db\pe_owner_snapshot.rs
```
pub struct PeSnapshotNode  :36-42
pub struct PeDbnumSnapshot  :45-49  # 单 dbnum 的内存层级快照（加载后只读，全部查询为纯内存同步操作）。
impl PeDbnumSnapshot  :89-256
  pub fn dbnum(&self) → u32  :90-90
  pub fn node_count(&self) → usize  :94-94
  pub fn contains_refno(&self, refno: RefU64) → bool  :98-98
  pub fn all_refnos(&self) → Vec<RefU64>  :102-102
  pub fn roots(&self) → &[RefU64]  :106-106
  pub fn node_meta(&self, refno: RefU64) → Option<TreeNodeMeta>  :121-121  # 与 `TreeIndex::node_meta` 同构（noun 为 db1_hash）。
  pub fn collect_children(&self, parent: RefU64, filter: &TreeQueryFilter) → Vec<RefU64>  :131-131  # 直接子节点（pe
  pub fn collect_descendants_bfs(&self, root: RefU64, options: &TreeQueryOptions) → Vec<RefU64>  :149-149  # BFS 收集子孙——语义逐行对齐 `TreeIndex::collect_descendants_bfs`
pub async fn get_or_load_pe_snapshot(dbnum: u32) → anyhow::Result<Arc<PeDbnumS...  :285-294  # 加载（或取缓存）单 dbnum 快照。并发调用只加载一次（OnceCell 收敛）。
pub fn try_get_cached_pe_snapshot(dbnum: u32) → Option<Arc<PeDbnumSnapshot>>  :297-301  # 同步取已加载快照（供 sync 闭包/循环使用；调用前须先 preload）。
pub async fn preload_pe_snapshots(dbnums: &[u32]) → anyhow::Result<()>  :304-309  # 预加载一组 dbnum 快照（sync 消费点之前调用）。
pub fn invalidate_pe_snapshots() → usize  :313-317  # 失效全部快照。**每次生成 run 开始必须调用**（gen_all_geos_data 入口），
pub fn invalidate_pe_snapshot(dbnum: u32)  :320-322  # 失效单 dbnum 快照。
```

### src\versioned_db\pe_owner_tree.rs
```
pub struct PeNodeMeta  :46-55  # 单节点层级元信息（latest 态；对齐 `TreeIndex::node_meta` 的消费面）。
pub struct PeOwnerTreeStore  :93-95  # pe_owner 图查询原语层。
impl PeOwnerTreeStore  :97-270
  pub fn new(dbnums: Vec<u32>) → Self  :98-98
  pub fn dbnums(&self) → &[u32]  :102-102
  pub async fn query_children(parent: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :113-113  # 查询直接子节点（同胞顺序 = 边 id `[owner, order]` 升序）。
  pub async fn query_children_filtered(parent: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :130-133  # 查询直接子节点并按 noun 过滤（保持同胞顺序）。
  pub async fn query_children_counts(refnos: &[RefnoEnum],) → anyhow::Result<HashMap<Refn...  :154-156  # 批量统计直接子节点数量（children_count 展示用）。
  pub async fn query_ancestors(node: RefnoEnum) → anyhow::Result<Vec<RefnoEnum>>  :190-190  # 查询祖先链，返回顺序与 TreeIndex 一致：根→父，不含自身。
  pub async fn query_ancestors_filtered(node: RefnoEnum, nouns: &[&str],) → anyhow::Result<Vec<RefnoEnum>>  :201-204  # 祖先链按 noun 过滤（保持根→父顺序）。
  pub async fn query_descendants(root: RefnoEnum, max_depth: Option<usize>,) → anyhow::Result<Vec<RefnoEnum>>  :230-233  # 查询全部子孙（不含自身），BFS 邻近序。
pub fn noun_hash_to_name(hash: u32) → String  :591-593  # noun_hash → noun 名称（分组结果转译用，与 TreeIndex 消费面同源）。
```

### src\versioned_db\version_commit.rs
```
pub struct VersionCommitCounts  :33-42
pub struct VersionCommitRequest  :45-55
pub struct VersionCommitOutcome  :58-68
pub struct ModelGenAnchor  :71-77
pub enum VersionCommitSource  :18-21
pub enum VersionCommitError  :87-143
impl VersionCommitSource  :23-30
pub fn compute_commit_fingerprint(dbnum: u32, from_sesno: u32, to_sesno: u32, source: VersionCommitSource, source_hash: Option<&str>, normalized_operations: impl IntoIterator<Item = &'a str>,) → String  :177-200
pub async fn commit_version(request: VersionCommitRequest, apply: F,) → CommitResult<VersionCommitO...  :202-211
pub async fn recover_version_commit(request: VersionCommitRequest, apply: F,) → CommitResult<VersionCommitO...  :213-222
pub async fn ensure_version_commit_schema() → CommitResult<()>  :361-408
pub async fn write_model_gen_anchor(dbnum: u32, sesno: u32) → anyhow::Result<ModelGenAnchor>  :765-767  # 发布模型生成完成锚点。
pub async fn write_model_gen_anchor_with_note(dbnum: u32, sesno: u32, note: &str,) → anyhow::Result<ModelGenAnchor>  :769-797
pub async fn current_dbnum_sesnos(dbnums: &[u32]) → anyhow::Result<BTreeMap<u32...  :800-826  # 读取指定 dbnum 在 `dbnum_info_table` 中的当前 sesno。
pub async fn publish_model_gen_anchors_after_generation(db_option: &crate::options::DbOptionExt, generation_success: bool, stage: &str, allow_all_when_unscoped: bool,) → anyhow::Result<Vec<ModelGen...  :848-894  # 在一次完整/手动模型生成及其后处理全部成功后，为参与 dbnum 发布 `model_gen` 锚点。
pub async fn ensure_live_generation_allowed(db_option: &crate::options::DbOptionExt, operation: &str,) → anyhow::Result<()>  :900-930  # Live/manual generation is only valid while building an initi
pub async fn committed_watermark(dbnum: u32) → anyhow::Result<u32>  :1005-1028  # specs/022 Committed Watermark：某 dbnum 已发布 Version Anchor 的最高
```

### src\web_api\e3d_tree_api.rs
```
pub struct E3dTreeApiState  :23-25
pub struct TreeNodeDto  :41-47
pub struct NodeResponse  :50-57
pub struct ChildrenResponse  :60-69
pub struct AncestorsResponse  :72-79
pub struct SubtreeRefnosResponse  :82-90
pub struct VisibleInstsResponse  :93-100
pub struct VisibleInstsDebug  :103-108
pub struct SearchRequest  :111-118
pub struct SearchResponse  :121-125
pub struct NodeAabb  :133-136  # AABB 包围盒
pub struct SiteNodeDto  :140-147  # Site Node 数据（用于前端构建 xeokit Node 层级）
pub struct SiteNodesResponse  :151-156  # Site Nodes API 响应
pub struct ChildrenQuery  :159-163
pub struct TreeVersionInfo  :171-178  # 带 `sesno` 的树接口响应附带的版本元信息（contracts/tree-version-api
pub struct ResolvedTreeVersion  :182-191  # 版本解析结果：锚点命中 + VERSION 时刻 + 数据源分界。
pub struct WorldRootQuery  :318-320
pub struct NodeQuery  :323-325
pub struct VersionGuardQuery  :329-331  # 版本模式不支持的接口用于拦截 sesno 参数（FR-010）。
pub struct SubtreeQuery  :637-643
pub struct AncestorsQuery  :646-649
pub enum TreeVersionError  :226-235  # 版本解析/查询错误 → HTTP 语义（对齐 `/api/model-history/*`，contracts 通用错误
impl ResolvedTreeVersion  :193-222
  pub fn use_pe_owner(&self) → bool  :195-195  # 数据源选择（FR-008）：可信分界之后走 pe_owner 边，否则回退 pe
  pub fn source_str(&self) → &'static str  :201-201
```

### src\web_api\mod.rs
```
pub fn assemble_stateless_web_api_routes() → axum::Router  :67-83  # 一次性装配所有"无状态" web_api 路由（含 nest 前缀）。
pub fn stateless_web_api_route_paths() → Vec<&'static str>  :98-180  # 与 [`assemble_stateless_web_api_routes`] 同步维护的静态路由路径清单。
```

### src\web_api\pdms_model_query_api.rs
```
pub struct RefnoQuery  :38-40
pub struct TypeInfoResponse  :43-50
pub struct ChildrenResponse  :53-58
pub fn create_pdms_model_query_routes() → Router  :22-26
```

### src\web_api\pipeline_annotation_api.rs
```
pub struct AnnotationResponse  :50-54  # 标注数据响应
pub struct AnnotationData  :58-65  # 标注数据
pub enum AnnotationCommand  :19-46  # 标注命令类型
pub fn create_pipeline_annotation_routes() → Router  :68-70  # 创建管道标注路由
```

### src\web_api\review_api.rs
```
pub struct CreateTaskRequest  :46-72  # 创建提资单请求
pub struct UpdateTaskRequest  :81-88  # 更新提资单请求
pub struct ReviewActionRequest  :92-95  # 审核操作请求
pub struct SubmitToNextRequest  :100-104  # 提交到下一节点请求
pub struct ReturnRequest  :109-114  # 驳回请求
pub struct WorkflowStep  :119-127  # 工作流步骤
pub struct ReviewComponent  :282-288  # 组件信息
pub struct ReviewAttachment  :293-299  # 附件信息
pub struct ReviewTask  :304-348  # 提资单
pub struct TaskListResponse  :546-552  # 任务列表响应
pub struct TaskResponse  :556-562  # 单个任务响应
pub struct ActionResponse  :566-572  # 操作响应
pub struct TaskListQuery  :576-587  # 查询参数
pub struct ConfirmedRecordData  :2719-2741  # 确认记录数据
pub struct ConfirmedRecordResponse  :2745-2753
pub struct ConfirmedRecordWithMeta  :2757-2772
pub struct AnnotationComment  :3459-3470  # 评论数据
pub struct CreateCommentRequest  :3474-3490
pub struct CommentResponse  :3493-3501
pub struct CommentQuery  :3504-3508
pub struct EditCommentRequest  :3804-3806
pub struct AnnotationSeverityTypeQuery  :3929-3932
pub struct AnnotationSeverityBody  :3936-3944
pub struct AnnotationSeverityResponse  :3947-3955
pub struct AnnotationBasicFieldsBody  :3967-3976
```

### src\web_api\room_tree_api.rs
```
pub struct RoomTreeNodeDto  :32-38
pub struct NodeResponse  :41-45
pub struct ChildrenResponse  :48-54
pub struct AncestorsResponse  :57-61
pub struct SearchRequest  :64-67
pub struct SearchResponse  :70-74
pub struct ChildrenQuery  :77-79
pub enum RoomTreeNodeId  :26-29
pub fn create_room_tree_routes() → Router  :454-463
pub async fn room_tree_children_core(id: &str, limit: usize) → anyhow::Result<ChildrenResp...  :468-622  # 不经 HTTP 层的核心逻辑：查询指定节点的子节点。
pub async fn room_tree_ancestors_core(id: &str) → anyhow::Result<AncestorsRes...  :630-766  # 不经 HTTP 层的核心逻辑：查询指定节点的祖先链。
pub async fn room_tree_search_core(keyword: &str, limit: usize) → anyhow::Result<SearchResponse>  :817-867  # 不经 HTTP 层的核心逻辑：按 keyword 搜索房间树（仅返回 ROOM 节点）。
```

### src\web_api\spatial_query_api.rs
```
pub struct SpatialQueryApiState  :17-19  # 空间查询API状态
pub struct SpatialNode  :33-39
pub struct SpatialQueryResponse  :42-47
pub struct NodeInfoResponse  :50-58
pub fn create_spatial_query_routes(state: SpatialQueryApiState) → Router  :22-28  # 创建空间查询路由
```

### src\web_server\handlers.rs
```
pub struct CreateBatchTaskRequest  :176-181  # 创建批量任务请求
pub struct CreateTaskTemplateRequest  :185-198  # 任务模板请求
pub struct SshOptions  :202-209  # SSH 连接参数
pub struct SurrealControlRequest  :213-229  # SurrealDB 控制请求，可选择本机或远程(SSH)
pub struct SurrealTestRequest  :233-240  # SurrealDB 连接测试请求
pub struct SqliteSpatialQuery  :2026-2036
pub struct DeploymentSiteBrowseQuery  :3193-3197
pub struct GetInstancesRequest  :3870-3872
pub struct ModelDataResponse  :3875-3879
pub struct SurrealStatusQuery  :4614-4617  # 查询 SurrealDB 运行状态
pub struct TraySupportsDetectRequest  :7119-7127
pub struct SctnTestRequest  :7258-7266
pub struct DatabaseConnectionStatus  :7622-7628  # 数据库连接状态
pub struct DatabaseConnectionConfig  :7632-7639  # 数据库连接配置
pub struct StartupScript  :7643-7649  # 启动脚本信息
pub struct DbConnCheckQuery  :7653-7660  # 检查数据库连接状态
pub struct StartDatabaseRequest  :7798-7801  # 数据库启动请求
pub struct ExportRequest  :8253-8274  # 导出请求结构体
pub struct ExportResponse  :8278-8283  # 导出响应结构体
pub struct ExportStatusResponse  :8287-8294  # 导出状态查询响应
pub struct ListFilesQuery  :9501-9504  # 获取指定 dbno 的 Parquet 文件列表
pub struct SceneTreeFileResponse  :9537-9545  # Scene Tree 文件响应
pub async fn kill_port_processes(port: u16) → Result<Vec<u32>, String>  :61-66  # 强制关闭占用端口的进程
pub async fn check_port_status(Query(params) → Result<Json<serde_json::Val...  :69-94  # 检查端口状态 API
pub async fn kill_port_processes_api(Json(req) → Result<Json<serde_json::Val...  :97-122  # 强制关闭端口占用进程 API
```

### src\web_server\incremental_update_handlers.rs
```
pub struct IncrementRunStatus  :37-49
pub struct ArchiveFile  :344-351
pub struct DbIncrementStatus  :447-459  # 每库增量/版本状态（真实数据）。
pub struct PendingCommitInfo  :462-467
pub struct IncrementalSyncQuery  :779-782  # 触发增量同步（真实落库，persist）：后台跑 IncrementRun，经 commit_version
pub async fn execute_incremental_update(_state: State<AppState>, Json(request) → impl IntoResponse  :212-341  # UI「增量更新」端点 `/api/db-status/update` 的真实实现。
pub async fn list_incremental_archives() → Result<Json<serde_json::Val...  :395-443  # 列出本地已生成的 CBA 归档包，供 collab monitor 的归档页面展示与下载。
pub async fn get_all_incremental_status(_state: State<AppState>) → impl IntoResponse  :579-605  # 全部 dbnum 的增量/版本状态（真实数据：锚点 + 水位 + Commit Pending）。
pub async fn get_site_incremental_details(_state: State<AppState>, Path(site_id) → impl IntoResponse  :622-721  # 单库增量详情：锚点时间线（最近 50 条）+ Commit Pending。
pub async fn start_incremental_detection(_state: State<AppState>, Path(site_id) → impl IntoResponse  :749-774  # 触发增量检测（只读试跑，no-persist）：后台跑 IncrementRun 收集变更但不落库。
pub async fn start_incremental_sync(_state: State<AppState>, Path(site_id) → impl IntoResponse  :784-810
pub async fn get_detection_task_status(_state: State<AppState>, Path(task_id) → impl IntoResponse  :813-830  # 查询某次增量运行（sync/detect）的状态。
pub async fn cancel_task(_state: State<AppState>, Path(_task_id) → impl IntoResponse  :834-841  # 取消增量任务：IncrementRun 一旦进入落库阶段不可安全中断，故不支持中途取消。
pub async fn get_incremental_config(_state: State<AppState>) → impl IntoResponse  :844-846  # 未实现：增量配置读取（无配置存储，检测/同步为按需触发）。
pub async fn update_incremental_config(_state: State<AppState>, Json(_config) → impl IntoResponse  :849-854  # 未实现：增量配置更新（无配置存储）。
```

### src\web_server\managed_project_sites.rs
```
pub struct RuntimeUpdate  :6008-6022
pub struct DbIndexRebuildSummary  :11938-11945  # db_index 预扫描结果摘要（用于 admin/CLI 反馈）。
pub struct StopSiteResult  :14767-14773
pub struct TailLogResponse  :15642-15650
impl QuickDeployProfile  :4092-4102
impl SidecarCliJobKind  :7633-7665
impl RoomComputeScopeSource  :7682-7690
impl DeployValidationReport  :8052-8071
impl RemoteExecutionMode  :12866-12885
pub fn quick_deploy_test_enabled() → bool  :286-293  # quick-deploy-test 免鉴权端点开关（默认关闭）。
pub fn scan_projects_under_root(raw_root: &str) → Result<ScanProjectsResult>  :1447-1450
pub fn ensure_schema() → Result<()>  :3059-3061
pub fn get_site_runtime_db_context(site_id: &str,) → Result<(ManagedProjectSite,...  :3178-3184  # 返回站点运行库连接所需的最小上下文。
pub fn list_remote_targets() → Result<Vec<ManagedRemoteTar...  :3555-3565
pub fn upsert_remote_target(req: ManagedRemoteTargetRequest) → Result<ManagedRemoteTarget>  :3567-3587
pub fn get_remote_deploy_status(site_id: &str) → Result<ManagedRemoteDeployS...  :3589-3636
pub fn get_site(site_id: &str) → Result<Option<ManagedProjec...  :3853-3862
pub fn list_sites() → Result<Vec<ManagedProjectSi...  :3864-3884
pub fn create_site(req: CreateManagedSiteRequest) → Result<ManagedProjectSite>  :3894-4021
pub async fn resolve_create_site_mbd_request(req: &mut CreateManagedSiteRequest) → Result<()>  :4799-4849
pub async fn resolve_update_site_mbd_request(site_id: &str, req: &mut UpdateManagedSiteRequest,) → Result<()>  :4851-4932
pub async fn quick_deploy_test(req: QuickDeployTestRequest) → Result<QuickDeployTestRespo...  :5022-5024  # 一键部署测试（免鉴权快测）：建站 → 解析(单库, 可选含关联) → 生成 →(可选)启动。
pub async fn quick_deploy_admin(req: QuickDeployTestRequest) → Result<QuickDeployTestRespo...  :5027-5040  # Admin 鉴权版 quick deploy：只快速创建部署配置，不自动解析/生成/启动。
pub fn preview_parse_plan(req: PreviewManagedSiteParsePlanRequest) → Result<ManagedSiteParsePlan>  :5614-5617
pub fn update_site(site_id: &str, req: UpdateManagedSiteRequest) → Result<ManagedProjectSite>  :5619-5744
```

### src\web_server\mod.rs
```
pub struct AppState  :153-174  # Web UI应用状态
pub struct TaskManager  :178-183  # 任务管理器
pub struct ConfigManager  :187-192  # 配置管理器
pub struct TaskQuery  :1502-1505  # 查询参数
pub struct CreateTaskRequest  :1509-1516  # 创建任务请求
pub struct UpdateConfigRequest  :1520-1522  # 更新配置请求
impl AppState  :194-253
  pub fn new() → Self  :195-195
impl ConfigManager  :275-279
  pub fn add_template(&mut self, name: &str, config: DatabaseConfig)  :276-276
pub fn get_local_ip_via_udp() → Result<String, std::io::Error>  :93-139
pub async fn start_web_server(port: u16) → anyhow::Result<()>  :282-284  # 启动Web UI服务器
pub async fn start_web_server_with_config(port: u16, config_file: Option<&str>,) → anyhow::Result<()>  :349-480
```

### src\web_server\model_runtime.rs
```
pub struct RealtimeInstancesRequest  :17-22
pub struct ParquetIncrementalEnqueueRequest  :25-28
pub struct ModelUnitVersionQuery  :31-33
pub fn ensure_runtime_started()  :35-39
pub async fn api_realtime_instances_by_refnos(Json(payload) → impl IntoResponse  :41-121
pub async fn api_parquet_incremental_enqueue(Json(payload) → impl IntoResponse  :123-135
pub async fn api_parquet_version(Path(dbno) → impl IntoResponse  :137-175
pub async fn api_model_unit_versions(Path(unit_refno) → impl IntoResponse  :177-197
pub async fn api_model_unit_version(Path((unit_refno, sesno) → impl IntoResponse  :199-223
pub async fn api_latest_model_unit_version(Path(unit_refno) → impl IntoResponse  :225-249
```

### src\web_server\models.rs
```
pub struct TaskInfo  :33-78  # 任务信息
pub struct TaskProgress  :138-153  # 任务进度
pub struct LogEntry  :157-169  # 日志条目
pub struct ErrorDetails  :173-188  # 错误详情
pub struct DatabaseConfig  :203-263  # 数据库配置
pub struct E3dProjectInfo  :439-457  # E3D项目信息
pub struct DeploymentSite  :461-532  # 部署站点
pub struct DeploymentSiteCreateRequest  :536-586  # 创建部署站点请求
pub struct DeploymentSiteImportRequest  :590-633  # 从 DbOption 导入部署站点的请求
pub struct DeploymentSiteUpdateRequest  :637-698  # 更新部署站点请求
pub struct DeploymentSiteQuery  :702-733  # 部署站点查询参数
pub struct DeploymentSiteTaskRequest  :737-749  # 部署站点任务配置请求
pub struct ManagedSiteParseHealth  :809-813  # 管理后台解析健康摘要
pub struct ManagedSiteParsePlan  :828-844  # 管理后台当前解析计划摘要
pub struct ParsePlanFact  :848-856  # sidecar 返回的单个解析目标事实，用于解释 included_db_files 的来源。
pub struct SiteProject  :889-903  # 站点内的单个工程条目（多工程合并站点的最小单元）
pub struct ScannedProject  :907-922  # 工程扫描候选项（Phase 3 扫描 API 返回的单个候选工程）
pub struct ScannedDbnumConflict  :926-929  # 跨候选工程的 dbnum 冲突（同一 dbnum 落在多个工程）
pub struct ScanProjectsResult  :933-942  # 工程扫描结果（Phase 3）
pub struct ManagedProjectSite  :946-1032  # 管理后台项目站点
pub struct ManagedSiteProcessResource  :1036-1041  # 管理后台单进程资源信息
pub struct ManagedSiteResourceMetrics  :1045-1058  # 管理后台站点资源信息
pub struct CreateManagedSiteRequest  :1062-1136  # 创建管理后台项目站点请求
pub struct UpdateManagedSiteRequest  :1140-1216  # 更新管理后台项目站点请求
pub struct PreviewManagedSiteParsePlanRequest  :1220-1257  # 管理后台解析计划预览请求
```

### src\web_server\stream_generate.rs
```
pub struct StreamGenerateRequest  :36-70  # 流式生成请求
pub struct StreamGenerateQuery  :818-833
pub enum StreamGenerateEvent  :91-139
pub async fn query_visible_descendants(root_refnos: &[RefnoEnum], max_depth: u32,) → anyhow::Result<Vec<RefnoEnum>>  :148-229  # 查询可见子节点（使用 aios_core 现有函数）
pub async fn filter_missing_inst_relate(refnos: &[RefnoEnum]) → anyhow::Result<Vec<RefnoEnum>>  :269-300  # 检查哪些 refno 还没有生成模型
pub async fn api_stream_generate(State(state) → Response  :309-429  # POST /api/model/stream-generate
pub async fn api_stream_generate_by_root(State(state) → Response  :838-854  # GET /api/model/stream-generate-by-root/{refno}
```
