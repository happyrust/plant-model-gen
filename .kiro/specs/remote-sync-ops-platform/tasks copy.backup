# Implementation Plan

## 远程同步运维平台实现任务

### 已完成的基础设施

- [x] 前端路由结构（`app/remote-sync/`）已创建
- [x] 后端 SSE 事件流（`sse_handlers.rs`）已实现
- [x] 基础 TypeScript 类型接口已定义
- [x] 部署向导组件（DeployWizard）已实现
- [x] 监控仪表板基础组件（EnvironmentCard、TaskList、MetricsPanel、AlertBanner）已实现
- [x] 监控页面（`app/remote-sync/monitor/page.tsx`）已实现并集成 SSE
- [x] React Flow 依赖已安装

### 待实现任务

- [ ] 1. 实现 CBA 文件分发服务
  - 在 `src/web_server/mod.rs` 中添加 `/assets/archives` 路由，使用 `ServeDir::new("assets/archives")` 提供静态文件服务
  - 验证 `assets/archives` 目录存在且可访问
  - 在元数据生成逻辑中使用 `file_server_host` 参数构建完整的 download_url（格式：`{file_server_host}/{filename}.cba`）
  - 测试通过 HTTP GET 请求下载 CBA 文件
  - _Requirements: 12.1, 12.2, 12.3, 12.4, 12.5_

- [ ] 2. 实现可视化拓扑配置功能
- [ ] 2.1 创建后端拓扑 API
  - 在 `src/web_server/` 下创建 `topology_handlers.rs` 文件
  - 实现 `TopologyData` 结构体（包含 environments、sites、connections）
  - 实现拓扑验证逻辑（validate 方法：检查环境必须有 MQTT/文件服务器配置、站点必须关联环境、数据库编号不为空）
  - 实现 GET `/api/remote-sync/topology` - 从数据库读取环境和站点，构建拓扑数据
  - 实现 POST `/api/remote-sync/topology` - 验证并保存拓扑配置到数据库
  - 实现 PUT `/api/remote-sync/topology` - 更新拓扑配置
  - 实现 DELETE `/api/remote-sync/topology/{id}` - 删除拓扑节点（级联删除关联）
  - 在 `mod.rs` 中注册拓扑 API 路由
  - _Requirements: 11.5, 11.8_

- [ ] 2.2 创建前端拓扑配置页面
  - 创建 `app/remote-sync/topology/page.tsx` 页面
  - 创建 `components/remote-sync/topology/topology-canvas.tsx` 组件（使用 React Flow）
  - 实现环境节点组件（EnvironmentNode）：显示名称、MQTT 地址、文件服务器地址、地区标识
  - 实现站点节点组件（SiteNode）：显示名称、HTTP 地址、所属环境、地区标识
  - 实现同步连线组件（SyncEdge）：有向箭头从环境指向站点
  - 实现节点拖拽、画布缩放和平移功能
  - 实现层次布局算法（环境节点在上层，站点节点在下层）
  - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.6_

- [ ] 2.3 实现拓扑配置面板和交互
  - 创建 `components/remote-sync/topology/node-config-panel.tsx` 侧边栏配置面板
  - 实现节点选中时显示详细配置表单（环境：名称、MQTT 地址/端口、文件服务器地址、地区标识、数据库编号列表、MQTT 重连参数；站点：名称、所属环境、地区标识、HTTP 地址、数据库编号列表、备注）
  - 实现实时编辑功能（修改后立即更新画布显示）
  - 实现工具栏（ToolbarCanvas）：添加环境节点、添加站点节点、创建连线、自动布局、导出 JSON、导入 JSON
  - 实现保存功能：调用后端 API 将拓扑转换为数据库记录
  - 实现加载功能：从后端 API 获取拓扑数据并渲染到画布
  - 实现删除节点功能：弹出确认对话框，显示关联站点数量，确认后级联删除
  - 实现导入/导出 JSON 功能
  - _Requirements: 11.5, 11.7, 11.8, 11.9_

- [ ] 3. 增强流向可视化功能
  - 实现后端流向统计 API（`/api/remote-sync/stats/flows`）：计算环境和站点之间的文件数量、总大小、平均速率、最近同步时间
  - 创建 `components/remote-sync/flow/flow-visualization.tsx` 组件（使用 React Flow）
  - 实现力导向图或桑基图布局
  - 实现节点渲染（环境节点和站点节点）
  - 实现流向连线渲染（显示流量大小、方向）
  - 实现鼠标悬停显示详细统计信息（文件数量、总大小、平均速率、最近同步时间）
  - 实现点击节点高亮相关流向
  - 实现时间范围筛选器（最近 1 小时/24 小时/7 天/30 天）
  - 实现异常流向标识（单向流量过大或长时间无数据）
  - 更新 `app/remote-sync/flow/page.tsx` 集成 FlowVisualization 组件
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. 实现日志查询功能
  - 创建 `components/remote-sync/logs/log-filters.tsx` 筛选条件组件（环境、站点、状态、方向、时间范围、关键词搜索）
  - 创建 `components/remote-sync/logs/log-table.tsx` 日志表格组件（使用 @tanstack/react-virtual 实现虚拟滚动）
  - 创建 `components/remote-sync/logs/log-detail.tsx` 日志详情抽屉组件（显示完整日志详情、文件路径、大小、记录数、耗时、错误信息、重试历史）
  - 实现日志导出功能（CSV/JSON 格式，限制单次导出不超过 10000 条）
  - 实现错误关键词高亮显示
  - 实现错误码解释和建议解决方案
  - 更新 `app/remote-sync/logs/page.tsx` 集成所有日志查询组件
  - 确保查询在 2 秒内返回结果，支持分页显示（每页 50 条，最多 500 条）
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_

- [ ] 5. 实现性能监控功能
  - 实现后端性能指标 API（`/api/sync/metrics`）：返回实时性能指标（同步速率、队列长度、活跃任务数、CPU 使用率、内存使用率）
  - 实现后端历史性能 API（`/api/sync/metrics/history`）：支持时间范围查询（最近 1 小时/24 小时/7 天/30 天）
  - 创建 `components/remote-sync/metrics/metric-card.tsx` 指标卡片组件
  - 创建 `components/remote-sync/metrics/trend-chart.tsx` 趋势图组件（使用 Recharts LineChart/AreaChart）
  - 创建 `components/remote-sync/metrics/statistics-panel.tsx` 统计面板组件（显示 P50/P95/P99 分位数）
  - 实现性能指标超过阈值时的警告状态显示
  - 实现性能报告导出功能（PDF/CSV 格式）
  - 更新 `app/remote-sync/metrics/page.tsx` 集成所有性能监控组件
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_

- [ ] 6. 实现站点元数据浏览功能
  - 创建 `components/remote-sync/metadata/metadata-info.tsx` 元数据信息组件
  - 创建 `components/remote-sync/metadata/file-entry-list.tsx` 文件条目列表组件
  - 实现文件下载功能（通过 download_url 下载 CBA 文件，显示下载进度条）
  - 实现刷新元数据功能（调用后端 API 强制刷新，refresh=true）
  - 实现错误处理（本地路径不存在/HTTP 不可达/缓存过期）
  - 实现重试和使用缓存的选项
  - 创建或更新 `app/remote-sync/[envId]/sites/[siteId]/page.tsx` 集成元数据浏览组件
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5_

- [ ] 7. 实现运维工具功能
  - 创建 `components/remote-sync/ops/ops-toolbar.tsx` 运维工具栏组件
  - 实现启动同步服务按钮（调用 `/api/sync/start`，显示启动结果）
  - 实现停止同步服务按钮（弹出确认对话框，调用 `/api/sync/stop`，等待任务完成或超时 30 秒）
  - 实现暂停同步按钮（调用 `/api/sync/pause`，暂停新任务但不中断运行中任务）
  - 实现恢复同步按钮（调用 `/api/sync/resume`）
  - 实现清空任务队列按钮（弹出二次确认对话框，调用 `/api/sync/queue/clear`，显示清空数量）
  - 创建 `components/remote-sync/ops/add-task-dialog.tsx` 手动添加任务对话框
  - 实现批量操作功能（批量暂停/恢复/取消任务）
  - 集成 OpsToolbar 到监控仪表板、日志查询、性能监控等页面
  - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5_

- [ ] 8. 实现告警和通知功能
  - 实现后端告警检测逻辑（在 `sync_control_center.rs` 中）：检测失败率超过 10%、MQTT 连接断开超过 5 分钟、队列积压超过 50 个任务
  - 实现告警事件广播（通过 SSE 推送告警事件）
  - 创建 `components/remote-sync/alerts/alert-notification.tsx` 告警通知组件
  - 实现告警历史记录功能（存储到数据库）
  - 实现告警规则配置功能（自定义阈值和通知方式）
  - 实现通知渠道（界面通知/邮件/Webhook）
  - 实现点击告警跳转到相关详情页面（自动填充筛选条件）
  - 创建 `app/remote-sync/alerts/page.tsx` 告警管理页面
  - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5_

- [ ] 9. 实现配置管理功能
  - 实现后端配置管理 API（`/api/sync/config` GET/PUT）：读取和更新同步配置（自动重试、最大重试次数、重试延迟、最大并发数、批量大小、同步间隔）
  - 创建 `components/remote-sync/config/config-form.tsx` 配置表单组件
  - 实现实时参数验证（最大并发数 1-20，重试次数 0-10，等）
  - 实现配置保存功能（调用后端 API，显示成功提示）
  - 实现重置为默认值功能（弹出确认对话框）
  - 实现配置历史记录功能（记录配置变更历史）
  - 更新 `app/remote-sync/config/page.tsx` 集成配置管理组件
  - _Requirements: 9.1, 9.2, 9.3, 9.4, 9.5_

- [ ] 10. 实现多环境管理功能
  - 创建 `components/remote-sync/environments/environment-list.tsx` 环境列表组件
  - 实现环境切换功能（调用 `/api/remote-sync/envs/{id}/activate`，停止当前环境运行时，启动新环境运行时）
  - 创建 `components/remote-sync/environments/environment-compare.tsx` 配置比较组件（并排显示差异项，高亮不同配置值）
  - 实现环境复制功能（创建新环境并复制源环境配置，除 ID 和名称外）
  - 实现环境删除功能（弹出确认对话框，显示关联站点数量，确认后级联删除）
  - 更新 `app/remote-sync/page.tsx` 集成多环境管理组件
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5_

- [ ]* 11. 编写测试
  - 编写前端单元测试（DeployWizard、TopologyCanvas、LogFilters、MetricsPanel 等组件）
  - 编写前端集成测试（部署流程、拓扑配置流程、日志查询流程）
  - 编写后端单元测试（拓扑验证逻辑、告警检测逻辑、配置验证逻辑）
  - 编写后端集成测试（拓扑 API、告警 API、配置 API）
  - 编写 E2E 测试（完整部署流程、拓扑配置流程、监控流程）
  - _Requirements: 所有需求_

- [ ]* 12. 性能优化
  - 实现前端代码分割（动态导入大型组件）
  - 配置 React Query 缓存策略（staleTime、cacheTime）
  - 实现虚拟滚动（LogTable、TaskList）
  - 优化数据库查询（添加索引、使用连接池）
  - 实现批量操作（批量插入日志）
  - 实现异步任务队列（使用 tokio mpsc 通道）
  - _Requirements: 所有需求_

- [ ]* 13. 编写文档
  - 编写用户文档（部署指南、使用手册、拓扑配置指南、故障排查）
  - 编写开发文档（架构说明、API 文档、组件文档、拓扑数据模型）
  - 配置 CI/CD 流程
  - 准备生产部署配置（Docker、systemd、监控、备份）
  - 确保 assets/archives 目录在部署时正确映射
  - _Requirements: 所有需求_
