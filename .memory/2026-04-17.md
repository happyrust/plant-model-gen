# 2026-04-17

## 01:54 [MCP-2] E3D 3.1 FM state READY 单字节永久补丁

- 完成：定位 core.dll 唯一写 F&M state (dword_6B73050) = 0x1C4 (INITing) 的指令 @ RVA 0x174DDA；将 imm32 的第 0 字节 C4→C2，让 state 启动时一次到 0x1C2 (READY)；Frida 运行时 + DLL 文件补丁双重验证通过；启动日志 StartEventLoop <<< OK；SGL 骨架 6 local_root + ~120 segment + check_hardware_features 全部跑完。
- 决策：选单字节改写而非 NOP 整条 mov，既保留调用图结构，又稳定 state=0x1C2，避免之前 force-write 0x1C2 被 reentrant 循环回写 0x1C4 的问题。
- 待办：3D 视图 FORM (GPH3DDESIGN1) 仍未自动创建，看不到 SGL_create_view/GLSTST；下一步排查 appFormCntrl.constructForms() 在 PML/.NET 层为何没注册显示该 FORM。

## 02:15 [MCP-2] PML modLoadMethod=true 补丁 + (61,119) guard 定位

- 完成：通过 start/fload/mload 插桩追踪 DES 启动 PML 执行链路；定位到 `PMLUI/DES/admin/start` 第 19 行 `!!modLoadMethod = false` 让 constructForms 跳过 GPH3DDESIGN 创建分支；将 false 改为 true 后 MACLOAD 执行到 constructForms 并进入 GPH3DDESIGN 分支；但 Form 定义被 (61,119) "Form definition is not allowed during form INITialisation" 错误拒绝。
- 决策：保留 start 的 modLoadMethod=true 补丁（影响面可控：只在首次启动且未定义时生效）；回滚 fload/mload 的 debug 代码保持原样；不盲目回滚旧 FM_FRMDEF_skip_guard，先用 Frida 定位真正 guard 位置。
- 待办：Frida hook MRMESS(61,119) backtrace，定位 core.dll 中仍存在的 form-init guard 指令位置并决定补丁策略。

## 11:40 [MCP-2] jmp-self 补丁实测失败 + 本日技术路线收敛

- 完成：
  1. 实现 `patch_jmpself_fminit.py`（apply/revert 两个模式）；
  2. 在 core.dll RVA 0x120894 写入 `EB FE`（原 `53 8B`），原字节备份到 `D:\reverse\e3d\fminit_entry_orig2bytes.bin`；
  3. 用 launch.bat 启动 + Frida 3s 后 attach — **des.exe 3 秒内就 exit**，没走到卡死循环。已 revert。
- 推测失败原因（三选一，无法进一步分辨）：
  - RVA 0x120894 实际不是 FMINIT 入口（反汇编看似是，但 VMP 或其它 wrapper 可能绕过）；
  - VMProtect 运行时完整性校验发现字节改变后 abort；
  - 主线程 jmp-self 被 watchdog/主 GUI 线程检测到 hang 后主动 exit。
- 今天技术路线彻底收敛。已尝试并失败的路径：
  1. 静态扫描 (61,119) 立即数 pattern — 0 对
  2. F&M struct diff — 候选字段是 coord transform 非 guard
  3. 常规 Frida attach — FMINIT 在 attach 前 0.2 秒就跑完
  4. frida.spawn(des.exe) — 立即 exit，`cmd /k` 依赖
  5. child_gating/spawn_gating — Windows 不支持
  6. **磁盘 jmp-self** — des.exe 提前 exit（上面的实测结论）
- 下一轮（新会话）建议方向：
  - 方向 A：**在 core.dll 更深处的函数而非 FMINIT 入口** 打 jmp-self（比如 FMINIT 内部的 `call 0x121B98` 之类），避开入口位置检测；
  - 方向 B：使用 `CREATE_SUSPENDED` Windows 原生 API 通过 Python ctypes 启动 des.exe 并直接控制 PC/断点；
  - 方向 C：完全换方向，深入 reverse engineer PML interpreter 找 `setup form` keyword 分发点，hook 那里而不是 F&M 层；
  - 方向 D：用 **e3d-license-hook/inject.py**（已经在 launch.bat 中配置的 E3D_HOOK_INJECTOR）这个已有注入器扩展功能，这是最"原生"的入口。

## 11:35 [MCP-2] spawn+suspend 阻塞 → "jmp self" 磁盘补丁方案

- 完成：
  1. 修好 env 传递（subprocess 捕获 evars.bat 输出后传给 frida.spawn(env=...)），但 **direct frida.spawn(des.exe) 仍使 des.exe 启动后立即 exit** — 说明 launch.bat 的 `cmd /k des.exe` 模式（父进程 cmd + keep-console）是 des.exe 必须的；
  2. 尝试 `frida.device.enable_spawn_gating()` 追踪 cmd 子进程 des.exe — **Windows 不支持** (`frida.NotSupportedError: not yet supported on this OS`)；
  3. 放弃 spawn gating 方案。
- 决策（下一轮实施）：**"jmp self" 磁盘补丁** 代替 spawn+suspend：
  1. 在 core.dll FMINIT 入口 RVA 0x120894 写入 2 字节 `EB FE`（覆盖原 `53 8B` = `push ebx; mov ebx,esp` 的前 2 字节），形成 `jmp $-0` 死循环；
  2. launch.bat 正常启动 des.exe，执行到 FMINIT 入口即卡死；
  3. Frida attach des.exe（无时间压力），hook FMINIT 之前先 dump F&M struct region → `pre_fminit`；
  4. Frida 内存写回原始 `53 8B` 两字节恢复 push ebx/mov ebx,esp 指令；
  5. 让 FMINIT 执行完毕后 dump `post_fminit`；diff 两次 dump 得到 FMINIT 活化的字段 = "in form INIT" 真正 flag 所在；
  6. dump 完成后记得回滚磁盘 core.dll 的 `EB FE` 补丁，保持正常 launch 可用；
  7. 针对 flag 地址设计永久补丁（强制在某指令上改 flag 初值或 NOP guard 分支）。
- 待办：
  1. 写 `patch_jmpself_fminit.py`：disk write `EB FE` @ raw(0x120894) + backup；
  2. 启动 launch.bat；Frida attach + dump + restore + resume；diff；
  3. 落地永久补丁。

## 11:27 [MCP-2] Frida spawn+suspend 机制验证

- 完成：写 `frida_spawn_des.py` + `launch_frida_spawn.bat`，通过 bat 先 call evars.bat 加载 AVEVA env 再 `frida.spawn(des.exe, args=["aps","SYSTEM/XXXXXX","/ALL"])` + suspend + attach + load JS + resume。**机制本身工作**：Hook FMINIT @ `core+0x120894` 成功 install，core.dll base 变化到 0x55eb0000。
- 阻塞点：des.exe 启动时报 `Unable to open the Attribute Data File - FNF:File not found %AVEVA_DESIGN_EXE%/attlib.dat`，`%AVEVA_DESIGN_EXE%` 未展开 — **des.exe 内部 PDMS env 解析器没收到 AVEVA env**。结果 des.exe 提前终止 native init，FMINIT 从未被调用（state=0x0 持续）。
- 决策：evars.bat 在 cmd /c 里 setlocal 的作用域可能阻断；需要用 `subprocess.run('cmd /c call evars.bat && set')` 捕获 env 然后传给 `frida.spawn(env=...)`；或者不用 launch.bat，通过 `frida-trace` / frida-inject 直接 suspend des.exe（attach 正常启动的 des.exe 但只是改 hook 时机）。
- 待办下一轮：修复 spawn env 传递（最简：手动构造完整 env dict 传给 frida.spawn），然后再跑，得到 FMINIT pre/post struct dump，diff 找出 "in form init" flag。

## 11:20 [MCP-2] WriteFile hook 排查 + Frida 方案总结

- 完成：Frida hook `kernel32!WriteFile` + 识别 macload_trace.log 句柄，捕获 PML writefile 的 backtrace。结果表明 **PML 所有 writefile 被 buffered，单次 flush 1495 字节完整日志**，backtrace 只指向 PML runtime 的 flush 函数（core+0xa22ec5/0xa28669/0xa20a3d/0xa34740），无法反推具体 (61,119) 产生位置。该策略失效。
- 决策：放弃从 writefile backtrace 反推 guard；改用 FSFDEF/FFSTUP/FZADFR **调用链深挖** + 或 **spawn+suspend** Frida attach（可以 hook 到 FMINIT entry/exit）。
- 关键结论：(61,119) 必发生在 ddfdata 的**第一次** setup form — 说明 F&M 启动时已经是 "in form INIT"。可能的根因是 FMINIT 内部的系统 Form（FMSYS/CALERT/CADCBTH 等）创建未正常完成，state 卡 INIT。
- 待办下一步（建议优先级）：
  1. PML 插桩：在 `ddfdata` 起头加 trace 打 `!!error.text` 和尝试调用 F&M reset，诊断 form-init 状态；
  2. Frida spawn+suspend：hook 住 FMINIT entry/exit 看 status 和 struct dump；  
  3. 搜 FMINIT 中的 system Form 创建函数（0x745390 / 0x749f10 等），hook 这些看是否失败；
  4. 评估 PML workaround: 在 ddfdata 的 `handle any` 里加一次 `$P FMEND` 或类似指令强制关闭 form definition，解锁死锁。

## 11:00 [MCP-2] F&M struct region 动态快照与 state 字段枚举

- 完成：
  1. 静态扫描：`[0x11A03050]` 是 F&M struct 的 state byte（offset 0x30），值 `0x1C4=INIT / 0x1C2=READY`。**确认之前的 0x174DDA 补丁生效** — Frida trace 显示 state 稳定保持 0x1C2。
  2. 动态 Frida dump 整个 `[0x11A02E00..0x11A03100]` struct region（~0x300 bytes），捕获早期和稳态两个快照。diff 得到 init 后新赋值字段：`[0x11A02F34]=0xFFFFFFFF`、`[0x11A03020..0x11A03040]` 含 coord transform doubles、`[0x11A0305C]=4`、`[0x11A03060]=3`、`[0x11A03064]=1`、`[0x11A030A0..0x11A030F0]` 为 C++ vtable ptr。
  3. 关键入口：RVA `0x174FA5-0x174FB9` 是单条连续的 init：`[0x1A0305C]=4; [0x1A03060]=3; [0x1A03064]=1`（紧贴 state setter 0x174DDA），说明这一组字段同出同入、用途关联。
  4. 级联死锁路径已清楚：`PMLUI/clib/files/ddfdata` 里 `var !x form ... display` → 触发 `(61,123)` → handle 分支 `setup form XXX` → **此处 setup form 报 `(61,119)`** → `handle any` 吞错但**无 `exit`** → form definition state 永卡 INIT → 后续所有 setup form 级联全失败。
  5. 确认 macload_trace.log 因 `openfile write` 对已存在文件静默失败导致不更新（删旧文件即可 recover）。
  6. 确认 (61,119) error number 不是编译时立即数：`.text` 里 mov imm 119 只有 1 处、61 有 6 处，两两配对 128 字节内 0 对。PML error (group, num) 由 native MRMESS 通过 register/variable 传递而非立即数。
- 决策：
  - `[0x11A03050]` state 补丁仍保留（不回滚），但确认它不是 (61,119) guard 源。
  - 下一步**不**继续静态搜 MRMESS 调用点（不可能找到）；应改用 Frida **Stalker** 跟踪 MACLOAD 期间 F&M 模块代码流，定位报 (61,119) 的实际指令序列。
  - 或优先更务实的 workaround：检查 `ddfdata` 补丁可行性（让 `handle any` 尝试强制发出 `exit` 重置 form-init 状态）。
- 待办：
  1. Frida Stalker / MemoryAccessMonitor 针对 `[0x11A03064]`（可能的"in form init"flag）精确定位写入点；
  2. 或 Stalker 跟踪 `setup form CD3DPOS` 的完整 native 执行路径直到 error setter；
  3. 评估 PML 层 `$P FMEND` 等指令是否能强制退出 form definition 作为 workaround。
- 关键文件：
  - `D:\reverse\e3d\fm_struct_dump_20260417_*.log`（struct 动态 dump）
  - `D:\reverse\e3d\candidate_refs.log`（candidate flag 的 refs）
  - `D:\reverse\e3d\macload_trace.log`（新一次 MACLOAD 执行 trace，7 个 setup form 全报 61,119）
  - `D:\AVEVA\Everything3D3.1\PMLUI\clib\files\ddfdata`（死锁源头）
