# 异地协同 v2 · Hi-Fi 原型 UI 完善计划 · 第五轮（最终轮）

> 制定日期：2026-04-21
>
> 前置：R1–R4 共 20 项已完成，原型 1814 行。
>
> 目标：最后一公里打磨——偏好持久化、快捷键、站点对比，使原型具备完整的用户体验闭环。

## 4 个改进项

### R5-P1 · localStorage 偏好持久化

**改动**：
1. 主题偏好 (light/dark) 持久化到 localStorage
2. 通知开关状态持久化
3. 上次选中的协同组 ID 持久化
4. 上次选中的 Tab 持久化
5. 配置表单修改持久化

**估时**：8 min

### R5-P2 · 快捷键面板

**改动**：
1. 按 `?` 键打开快捷键帮助面板
2. 快捷键列表：`1-4` 切换 Tab、`/` 聚焦搜索、`Esc` 关闭抽屉/全屏、`F` 拓扑全屏
3. 面板用 Modal 样式，Escape 关闭

**估时**：10 min

### R5-P3 · 站点对比视图

**改动**：
1. 站点 Tab 添加「对比」模式按钮
2. 对比模式下可勾选 2 个站点
3. 底部弹出对比面板（并排展示：文件数/记录数/健康度/最近同步/状态机）
4. 差异项高亮显示

**估时**：15 min

### R5-P4 · 拓扑视觉微调

**改动**：
1. 流向连线添加虚线动画（bad 流向脉冲闪烁效果）
2. 节点内添加迷你进度条（有 progress 时显示）
3. 选中节点时其他节点降低透明度

**估时**：8 min

## 总估时 ~41 min
