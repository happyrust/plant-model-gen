#V2
SELECT count() FROM tubi_relate;
USE NS n1500 DB YCYK_E3D;
-- 查看所有表
INFO FOR DB;
-- 查找包含 tubi 的表
USE NS n1500 DB YCYK_E3D;
-- 列出所有表
USE NS n1500 DB YCYK_E3D;
-- 检查 tubi_relate 表是否存在
INFO FOR DB;
-- 如果表存在，查看表结构
INFO FOR TABLE tubi_relate;
-- 查看所有表名
SELECT * FROM information_schema.tables WHERE table_name = 'tubi_relate';
USE NS n1500 DB YCYK_E3D;
-- 查看是否有任何 TUBI 数据
SELECT * FROM pe WHERE noun = 'TUBI' LIMIT 5;
-- 查看是否有任何 tubi_relate 关系
SELECT * FROM tubi_relate LIMIT 5;
-- 查看是否有任何包含 'tubi' 的表
USE NS n1500 DB YCYK_E3D;
-- 查看是否有任何 TUBI 数据
SELECT * FROM pe WHERE noun = 'TUBI' LIMIT 5;
-- 查看所有表
USE NS n1500 DB YCYK_E3D;
-- 查看是否有任何 TUBI 数据
SELECT * FROM pe WHERE noun = 'TUBI' LIMIT 5;
-- 查看 BRAN 数据
SELECT * FROM pe WHERE noun = 'BRAN' LIMIT 5;
-- 查看 HANG 数据
SELECT * FROM pe WHERE noun = 'HANG' LIMIT 5;
USE NS n1500 DB YCYK_E3D;
-- 查看数据库中是否有任何数据
SELECT count() FROM pe;
-- 查看所有不同的 noun 类型
SELECT noun FROM pe GROUP BY noun LIMIT 10;
-- 查看命名空间
INFO FOR NS;
-- 查看数据库
INFO FOR DB;
-- 尝试使用另一个数据库
USE NS n1500 DB YCYK_E3D;
-- 查看数据库中是否有任何数据
SELECT count() FROM pe;
-- 查看所有不同的 noun 类型
SELECT noun FROM pe GROUP BY noun LIMIT 10;
-- 查看所有命名空间
INFO FOR ROOT;
USE NS n1500 DB YCYK_E3D;
-- 查看数据库中是否有任何数据
SELECT count() FROM pe;
-- 查看所有不同的 noun 类型
SELECT noun FROM pe GROUP BY noun LIMIT 10;
-- 查看 n1500 命名空间下的所有数据库
INFO FOR NS;
USE NS n1500 DB YCYK_E3D;
-- 查看数据库中是否有任何数据
SELECT count() FROM pe;
-- 查看所有不同的 noun 类型
SELECT noun FROM pe GROUP BY noun LIMIT 10;
-- 直接查询数据库中的数据，不指定命名空间和数据库
SELECT count() FROM pe;
-- 查看所有不同的 noun 类型
SELECT noun FROM pe GROUP BY noun LIMIT 10;
SELECT count() FROM pe;
INFO FOR DB;
INFO FOR ROOT;
INFO FOR DB;
USE NS n1500 DB YCYK_E3D;
-- 查看数据库中是否有任何数据
SELECT count() FROM pe;
-- 查看所有不同的 noun 类型
SELECT noun FROM pe GROUP BY noun LIMIT 10;
USE NS n1500 DB YCYK_E3D;
-- 检查 tubi_relate 表是否存在
INFO FOR DB;
-- 如果表存在，查看表结构
INFO FOR TABLE tubi_relate;
-- 查看所有表名
SELECT * FROM information_schema.tables WHERE table_name = 'tubi_relate';
INFO FOR DB;
SELECT * FROM pe LIMIT 5;
INFO FOR DB;
-- 检查 tubi_relate 表是否存在
INFO FOR DB;
-- 如果表存在，查看表结构
INFO FOR TABLE tubi_relate;
-- 查看所有表名
SELECT * FROM information_schema.tables WHERE table_name = 'tubi_relate';
-- 简单查询 tubi_relate 表
SELECT * FROM tubi_relate LIMIT 5;
SELECT in as refno, in.sesno as sesno, in.noun as noun, world_trans.d as wt, aabb.d as aabb FROM inst_relate:30101_14380 WHERE in.id != none and !bad_bool and aabb.d != NONE;
exit
SELECT in as refno, in.sesno as sesno, in.noun as noun, world_trans.d as wt, aabb.d as aabb FROM inst_relate:30101_14380 WHERE in.id != none and !bad_bool and aabb.d != NONE;
-- 查询负实体关系
SELECT * FROM array::flatten(pe:30101_14380<-neg_relate.in<-inst_relate);
SELECT * FROM array::flatten(pe:30101_14380<-ngmr_relate.in->inst_relate);
-- 检查是否存在正实体几何
SELECT out as id, trans.d as trans FROM inst_relate:30101_14380.out->geo_relate WHERE geo_type in ["Compound", "Pos"] and trans.d != NONE;
INFO FOR DB;
