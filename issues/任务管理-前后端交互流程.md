# 任务管理 - 前后端数据交互流程详解

## 📋 文档说明

本文档详细描述任务管理功能的前后端数据交互流程，包括请求参数、响应数据结构、业务逻辑流程等。

---

## 1️⃣ 任务创建流程

### 前端 → 后端

#### 请求
```http
POST /api/tasks
Content-Type: application/json

{
  "name": "AvevaMarineSample-任务-0001",
  "task_type": "FullGeneration",
  "config": {
    "name": "自定义配置",
    "manual_db_nums": [7999],
    "gen_model": true,
    "gen_mesh": true,
    "gen_spatial_tree": true,
    "apply_boolean_operation": true,
    "mesh_tol_ratio": 3.0,
    "room_keyword": "-RM",
    "project_name": "AvevaMarineSample",
    "project_code": 1516
  }
}
```

#### 后端处理逻辑

**文件**: `src/web_ui/handlers.rs:1373-1396`

```rust
pub async fn create_task(
    State(state): State<AppState>,
    Json(request): Json<CreateTaskRequest>,
) -> Result<Json<TaskInfo>, (StatusCode, Json<serde_json::Value>)> {
    // 1. 验证任务名称
    if request.name.trim().is_empty() {
        return Err((
            StatusCode::BAD_REQUEST,
            Json(json!({"error": "任务名称不能为空"})),
        ));
    }

    // 2. 创建任务对象
    let mut task_manager = state.task_manager.lock().await;
    let task = TaskInfo::new(request.name, request.task_type, request.config);
    let task_id = task.id.clone();

    // 3. 存储到活跃任务列表
    task_manager.active_tasks.insert(task_id, task.clone());

    // 4. 返回任务信息
    Ok(Json(task))
}
```

#### 响应
```json
{
  "id": "site_AvevaMarineSample_task_20251024_123456_abc123",
  "name": "AvevaMarineSample-任务-0001",
  "task_type": "FullGeneration",
  "status": "Pending",
  "config": { /* 配置信息 */ },
  "created_at": 1729756800000,
  "started_at": null,
  "completed_at": null,
  "progress": {
    "current_step": "等待启动",
    "total_steps": 0,
    "current_step_number": 0,
    "percentage": 0.0,
    "processed_items": 0,
    "total_items": 0,
    "estimated_remaining_seconds": null
  },
  "error": null,
  "error_details": null,
  "logs": [],
  "priority": "Normal",
  "dependencies": [],
  "estimated_duration": null,
  "actual_duration": null
}
```

---

## 2️⃣ 任务启动流程

### 前端 → 后端

#### 请求
```http
POST /api/tasks/{task_id}/start
```

#### 后端处理逻辑

**文件**: `src/web_ui/handlers.rs:1399-1431`

```rust
pub async fn start_task(
    Path(id): Path<String>,
    State(state): State<AppState>,
) -> Result<Json<serde_json::Value>, StatusCode> {
    let mut task_manager = state.task_manager.lock().await;

    if let Some(task) = task_manager.active_tasks.get_mut(&id) {
        // 1. 检查任务状态
        if task.status == TaskStatus::Pending {
            // 2. 更新任务状态
            task.status = TaskStatus::Running;
            task.started_at = Some(SystemTime::now());
            task.add_log(LogLevel::Info, "任务开始执行".to_string());

            // 3. 启动异步任务执行
            let state_cp = state.clone();
            let id_cp = id.clone();
            tokio::spawn(async move {
                // 获取信号量（限制并发数）
                let _permit = TASK_EXEC_SEMAPHORE
                    .clone()
                    .acquire_owned()
                    .await
                    .expect("semaphore");
                // 执行实际任务
                execute_real_task(state_cp, id_cp).await;
            });

            return Ok(Json(json!({
                "success": true,
                "message": "任务已启动"
            })));
        }
    }

    Err(StatusCode::NOT_FOUND)
}
```

#### 任务执行流程

**文件**: `src/web_ui/handlers.rs:3700-4309`

```rust
async fn execute_real_task(state: AppState, task_id: String) {
    // 1. 获取任务信息
    let (task_type, config) = {
        let task_manager = state.task_manager.lock().await;
        let task = task_manager.active_tasks.get(&task_id);
        // ...
    };

    // 2. 根据任务类型执行不同逻辑
    match task_type {
        TaskType::ParsePdmsData | TaskType::DataParsingWizard => {
            // 仅解析PDMS数据
            // 步骤1: 解析PDMS数据
            // 步骤2: 标记完成
        }
        TaskType::FullGeneration => {
            // 完整生成流程
            // 步骤1: 解析PDMS数据
            // 步骤2: 生成几何数据
            // 步骤3: 生成空间树
            // 步骤4: 标记完成
        }
        // ... 其他任务类型
    }

    // 3. 更新任务进度
    // 4. 记录日志
    // 5. 处理错误
    // 6. 移入历史记录
}
```

#### 响应
```json
{
  "success": true,
  "message": "任务已启动"
}
```

---

## 3️⃣ 任务状态监控流程

### 前端轮询机制

```typescript
// 推荐实现
useEffect(() => {
  const fetchTaskStatus = async () => {
    try {
      const response = await fetch(`/api/tasks/${taskId}`)
      const task = await response.json()
      setTaskStatus(task.status)
      setProgress(task.progress)
      setLogs(task.logs)
    } catch (error) {
      console.error('获取任务状态失败:', error)
    }
  }

  // 初始加载
  fetchTaskStatus()

  // 每5秒轮询一次
  const interval = setInterval(fetchTaskStatus, 5000)

  // 清理定时器
  return () => clearInterval(interval)
}, [taskId])
```

### 后端 → 前端

#### 请求
```http
GET /api/tasks/{task_id}
```

#### 响应（运行中的任务）
```json
{
  "id": "site_AvevaMarineSample_task_20251024_123456_abc123",
  "name": "AvevaMarineSample-任务-0001",
  "task_type": "FullGeneration",
  "status": "Running",
  "config": { /* ... */ },
  "created_at": 1729756800000,
  "started_at": 1729756810000,
  "completed_at": null,
  "progress": {
    "current_step": "生成几何数据",
    "total_steps": 3,
    "current_step_number": 2,
    "percentage": 66.7,
    "processed_items": 5000,
    "total_items": 7500,
    "estimated_remaining_seconds": 120
  },
  "error": null,
  "error_details": null,
  "logs": [
    {
      "timestamp": 1729756810000,
      "level": "Info",
      "message": "任务开始执行",
      "error_code": null,
      "stack_trace": null
    },
    {
      "timestamp": 1729756820000,
      "level": "Info",
      "message": "PDMS数据解析完成",
      "error_code": null,
      "stack_trace": null
    },
    {
      "timestamp": 1729756830000,
      "level": "Info",
      "message": "开始生成几何数据",
      "error_code": null,
      "stack_trace": null
    }
  ],
  "priority": "Normal",
  "dependencies": [],
  "estimated_duration": 300,
  "actual_duration": null
}
```

---

## 4️⃣ 任务日志查看流程

### 前端 → 后端

#### 请求（带过滤参数）
```http
GET /api/tasks/{task_id}/logs?level=Error&limit=50&search=失败
```

#### 查询参数说明
```typescript
interface TaskLogQuery {
  level?: 'Debug' | 'Info' | 'Warning' | 'Error' | 'Critical'
  limit?: number
  search?: string
}
```

#### 后端处理逻辑

**文件**: `src/web_ui/handlers.rs:1315-1370`

```rust
pub async fn get_task_logs(
    Path(id): Path<String>,
    Query(params): Query<TaskLogQuery>,
    State(state): State<AppState>,
) -> Result<Json<serde_json::Value>, StatusCode> {
    let task_manager = state.task_manager.lock().await;

    // 1. 查找任务
    let task = if let Some(task) = task_manager.active_tasks.get(&id) {
        task
    } else if let Some(task) = task_manager.task_history.iter().find(|t| t.id == id) {
        task
    } else {
        return Err(StatusCode::NOT_FOUND);
    };

    // 2. 过滤日志
    let mut logs: Vec<&LogEntry> = task.logs.iter().collect();

    // 按级别过滤
    if let Some(level_filter) = &params.level {
        logs.retain(|log| match level_filter.as_str() {
            "Debug" => matches!(log.level, LogLevel::Debug),
            "Info" => matches!(log.level, LogLevel::Info),
            "Warning" => matches!(log.level, LogLevel::Warning),
            "Error" => matches!(log.level, LogLevel::Error),
            "Critical" => matches!(log.level, LogLevel::Critical),
            _ => true,
        });
    }

    // 按关键词搜索
    if let Some(search_query) = &params.search {
        let query_lower = search_query.to_lowercase();
        logs.retain(|log| log.message.to_lowercase().contains(&query_lower));
    }

    // 限制数量
    if let Some(limit) = params.limit {
        logs.truncate(limit);
    }

    // 3. 返回结果
    Ok(Json(json!({
        "task_id": task.id,
        "task_name": task.name,
        "logs": logs,
        "total_logs": task.logs.len(),
        "filtered_count": logs.len()
    })))
}
```

#### 响应
```json
{
  "task_id": "site_AvevaMarineSample_task_20251024_123456_abc123",
  "task_name": "AvevaMarineSample-任务-0001",
  "logs": [
    {
      "timestamp": 1729756850000,
      "level": "Error",
      "message": "几何数据生成失败: 无法找到指定的数据库文件",
      "error_code": "GEO_GEN_001",
      "stack_trace": "at generate_geometry() line 123\nat execute_task() line 456"
    }
  ],
  "total_logs": 25,
  "filtered_count": 1
}
```

---

## 5️⃣ 批量任务创建流程

### 前端 → 后端

#### 请求
```http
POST /api/tasks/batch
Content-Type: application/json

{
  "template_id": "parse_pdms_single",
  "batch_config": {
    "name_prefix": "批量解析任务",
    "db_nums": [7001, 7002, 7003, 7004, 7005],
    "parallel_execution": false,
    "max_concurrent": 2,
    "continue_on_failure": true
  }
}
```

#### 后端处理逻辑

**文件**: `src/web_ui/handlers.rs:2084-2145`

```rust
pub async fn create_batch_tasks(
    State(state): State<AppState>,
    Json(request): Json<CreateBatchTaskRequest>,
) -> Result<Json<serde_json::Value>, StatusCode> {
    let mut task_manager = state.task_manager.lock().await;

    // 1. 验证模板
    let templates = get_task_templates(State(state.clone())).await?;
    let template = templates.0.iter()
        .find(|t| t.id == request.template_id)
        .ok_or(StatusCode::BAD_REQUEST)?;

    let mut created_tasks = Vec::new();
    let mut previous_task_id = None;

    // 2. 为每个数据库创建任务
    for (i, db_num) in request.batch_config.db_nums.iter().enumerate() {
        let task_name = format!("{} - 数据库 {}", 
            request.batch_config.name_prefix, db_num);

        // 3. 配置任务
        let mut config = template.default_config.clone();
        config.manual_db_nums = vec![*db_num];
        config.name = task_name.clone();

        let mut task = TaskInfo::new(task_name, template.task_type.clone(), config);
        task.estimated_duration = template.estimated_duration;

        // 4. 添加依赖关系（串行执行）
        if !request.batch_config.parallel_execution {
            if let Some(prev_id) = previous_task_id.clone() {
                task.dependencies.push(prev_id);
            }
        }

        created_tasks.push(json!({
            "id": task.id,
            "name": task.name,
            "db_num": db_num,
            "dependencies": task.dependencies
        }));

        previous_task_id = Some(task.id.clone());
        task_manager.active_tasks.insert(task.id.clone(), task);
    }

    // 5. 返回结果
    Ok(Json(json!({
        "success": true,
        "message": format!("成功创建 {} 个任务", created_tasks.len()),
        "tasks": created_tasks,
        "total_count": created_tasks.len()
    })))
}
```

#### 响应
```json
{
  "success": true,
  "message": "成功创建 5 个任务",
  "tasks": [
    {
      "id": "site_default_task_20251024_123456_001",
      "name": "批量解析任务 - 数据库 7001",
      "db_num": 7001,
      "dependencies": []
    },
    {
      "id": "site_default_task_20251024_123457_002",
      "name": "批量解析任务 - 数据库 7002",
      "db_num": 7002,
      "dependencies": ["site_default_task_20251024_123456_001"]
    },
    // ... 其他任务
  ],
  "total_count": 5
}
```

---

## 6️⃣ 任务列表查询流程

### 前端 → 后端

#### 请求（带过滤参数）
```http
GET /api/tasks?status=Running&limit=20
```

#### 查询参数说明
```typescript
interface TaskQuery {
  status?: 'pending' | 'running' | 'completed' | 'failed' | 'cancelled'
  limit?: number
}
```

#### 后端处理逻辑

**文件**: `src/web_ui/handlers.rs:1231-1261`

```rust
pub async fn get_tasks(
    Query(params): Query<TaskQuery>,
    State(state): State<AppState>,
) -> Result<Json<serde_json::Value>, StatusCode> {
    let task_manager = state.task_manager.lock().await;

    // 1. 合并活跃任务和历史任务
    let mut tasks: Vec<&TaskInfo> = task_manager.active_tasks.values().collect();
    tasks.extend(task_manager.task_history.iter());

    // 2. 按状态过滤
    if let Some(status_filter) = &params.status {
        tasks.retain(|task| match status_filter.as_str() {
            "pending" => task.status == TaskStatus::Pending,
            "running" => task.status == TaskStatus::Running,
            "completed" => task.status == TaskStatus::Completed,
            "failed" => task.status == TaskStatus::Failed,
            "cancelled" => task.status == TaskStatus::Cancelled,
            _ => true,
        });
    }

    // 3. 限制数量
    if let Some(limit) = params.limit {
        tasks.truncate(limit);
    }

    // 4. 返回结果
    Ok(Json(json!({
        "tasks": tasks,
        "total": tasks.len()
    })))
}
```

#### 响应
```json
{
  "tasks": [
    {
      "id": "site_AvevaMarineSample_task_20251024_123456_abc123",
      "name": "AvevaMarineSample-任务-0001",
      "task_type": "FullGeneration",
      "status": "Running",
      "progress": {
        "percentage": 45.5,
        "current_step": "生成几何数据"
      },
      // ... 其他字段
    },
    // ... 更多任务
  ],
  "total": 3
}
```

---

## 7️⃣ 错误处理流程

### 任务执行失败

#### 后端错误处理

```rust
// 捕获错误并记录详情
if let Err(e) = generate_geometry_result {
    let error_code = "GEO_GEN_001";
    let solutions = vec![
        "检查数据库文件是否存在".to_string(),
        "验证数据库编号是否正确".to_string(),
        "确认项目路径配置正确".to_string(),
    ];

    let error_details = ErrorDetails {
        error_type: "GeometryGenerationError".to_string(),
        error_code: Some(error_code.to_string()),
        failed_step: "生成几何数据".to_string(),
        detailed_message: format!("几何数据生成过程中发生错误: {}", e),
        stack_trace: Some(format!("{:?}", e)),
        suggested_solutions: solutions,
        related_config: Some(json!({
            "manual_db_nums": config.manual_db_nums,
            "gen_model": config.gen_model,
            "mesh_tol_ratio": config.mesh_tol_ratio
        })),
    };

    task.set_error_details(error_details);
    task.status = TaskStatus::Failed;
}
```

#### 前端获取错误详情

```http
GET /api/tasks/{task_id}/error
```

#### 响应
```json
{
  "task_id": "site_AvevaMarineSample_task_20251024_123456_abc123",
  "task_name": "AvevaMarineSample-任务-0001",
  "error": "几何数据生成失败",
  "error_details": {
    "error_type": "GeometryGenerationError",
    "error_code": "GEO_GEN_001",
    "failed_step": "生成几何数据",
    "detailed_message": "几何数据生成过程中发生错误: 无法找到数据库文件",
    "stack_trace": "at generate_geometry() line 123...",
    "suggested_solutions": [
      "检查数据库文件是否存在",
      "验证数据库编号是否正确",
      "确认项目路径配置正确"
    ],
    "related_config": {
      "manual_db_nums": [7999],
      "gen_model": true,
      "mesh_tol_ratio": 3.0
    }
  },
  "error_logs": [
    {
      "timestamp": 1729756850000,
      "level": "Error",
      "message": "几何数据生成失败: 无法找到指定的数据库文件"
    }
  ],
  "all_logs": [ /* 所有日志 */ ]
}
```

---

## 📊 数据流程图

```
┌─────────────┐
│   前端UI    │
└──────┬──────┘
       │
       │ 1. 创建任务
       ▼
┌─────────────────────┐
│  POST /api/tasks    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  create_task()      │
│  - 验证参数         │
│  - 创建TaskInfo     │
│  - 存入active_tasks │
└──────┬──────────────┘
       │
       │ 返回TaskInfo
       ▼
┌─────────────┐
│   前端UI    │
│  显示任务   │
└──────┬──────┘
       │
       │ 2. 启动任务
       ▼
┌──────────────────────────┐
│  POST /api/tasks/{id}/start │
└──────┬───────────────────┘
       │
       ▼
┌─────────────────────┐
│  start_task()       │
│  - 更新状态Running  │
│  - 启动异步执行     │
└──────┬──────────────┘
       │
       │ 异步执行
       ▼
┌─────────────────────┐
│  execute_real_task()│
│  - 解析PDMS数据     │
│  - 生成几何数据     │
│  - 生成空间树       │
│  - 更新进度         │
│  - 记录日志         │
└──────┬──────────────┘
       │
       │ 3. 轮询状态（每5秒）
       ▼
┌─────────────────────┐
│  GET /api/tasks/{id}│
└──────┬──────────────┘
       │
       │ 返回最新状态
       ▼
┌─────────────┐
│   前端UI    │
│  更新进度   │
│  显示日志   │
└─────────────┘
```

---

## 🔧 实现建议

### 前端实现要点

1. **状态管理**: 使用React Context或Zustand管理任务状态
2. **实时更新**: 使用轮询或WebSocket实时更新任务状态
3. **错误处理**: 统一的错误提示和重试机制
4. **性能优化**: 虚拟滚动处理大量任务列表
5. **用户体验**: 加载状态、骨架屏、乐观更新

### 后端优化建议

1. **持久化**: 将任务数据持久化到数据库（目前仅内存）
2. **任务队列**: 实现真正的任务队列系统
3. **并发控制**: 优化Semaphore使用，支持动态调整
4. **日志管理**: 日志分级存储，避免内存溢出
5. **监控告警**: 添加任务执行监控和异常告警

---

**文档版本**: 1.0  
**最后更新**: 2025-10-24

